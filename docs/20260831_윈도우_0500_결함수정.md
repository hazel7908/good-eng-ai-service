# Windows 세션 지시서 — 소환 마무리(0500) + 결함 2건 + 다음 영역 수확 (2026-08-31 심야)

> 앞 지시서(`20260831_윈도우_베이스대기열.md`)는 **완료** — 베이스 5개·되먹임까지 끝났다.
> **소환 베이스는 10파트가 아니라 11파트다** — 0500 환경현황이 spec 부재로 보류였고
> (앞 지시서 ③ "보내지 마라"), Mac 이 이번에 체인을 완성했다. 이 지시서 ③으로 찍으면
> 소환 베이스 진짜 전부 완료 → ⑤에서 **다음 영역(W3 어려운 파트 + 재해 3종) 재료 수확**으로
> 넘어간다. **시작 전에 `git pull`.** (Mac 은 대여 장비로 교체됐다 — 채점·명세는 계속 Mac 몫.)

---

## 0. 먼저 알아야 할 것 — Mac 이 8건을 채점했다

| 사업 | 파트 | OK+문형 | WRONG | 분류 |
|---|---|:--:|:--:|---|
| 원주 | 수질 (재채점) | 45.7→**57.1%** | 5 | 전부 채점기 아티팩트* + vars 오류(아래 ②) |
| 천안 | 수질 (재채점) | 21.6% | 2 | 채점기 아티팩트* (vars 는 정직한 null) |
| 원주 | 사업개요 0100 | 33.3% | 1 | 아티팩트* |
| 원주 | 대상지역 0300 | 75.0% | 2 | 아티팩트* |
| 원주 | 주변토지 0400 | 66.7% | 0 ✅ | |
| 원주 | 토지이용 0724 | 66.7% | 1 | 아티팩트* |
| 천안 | 사업개요 0100 | 0.0% | 0 ✅ | vars 러프 (잉여 9는 절 짝짓기 문제) |
| 천안 | 자원순환 0726 | 19.2% | **7** | **5건 실결함(아래 ①)** + 2 아티팩트* |

\* 아티팩트 = 채점기 `gold_table` 이 정답 블록을 잘못 자른다. 셀 값 `2.0` 을 절 번호
`2.N` 으로 오인해 끊거나, 파트식 소제목(`라. 허가권자`·`(1) 토지이용계획`)에서 안 끊어
다음 표 숫자를 삼킨다. **Mac 후속** — Windows 는 신경 쓸 것 없다.

---

## ① 실결함 — 천안 0726 성상별 5표에 원주 폐기물 통계가 그대로 실렸다 ★

`868`(관리구역㎢)·`366,306`(인구)·`1,949`(배출량) … 전부 **원주시 값**이다.
핸들러가 "값 없음, 스킵" 으로 두었던 바로 그 자리 (CLAUDE.md 함정: **자료가 없으면
건너뛰지 말고 비운다**).

**Mac 이 핸들러를 고쳐 두었다** (`engine/parts/small-env/resource-cycle.py` —
성상별 5표 선비우기: `원주시` 라벨 행을 비우고 라벨만 새 시군으로. 되먹임 대비
skip=i 방어 포함). **실행 검증은 안 됐다.**

### 할 일

```bash
python engine/generate.py small-env resource-cycle 원주_무장리   # 되먹임 먼저
python engine/smoke_check.py small-env resource-cycle 원주_무장리
python engine/generate.py small-env resource-cycle 천안_화덕리
python engine/smoke_check.py small-env resource-cycle 천안_화덕리
```

- 원주 되먹임이 없다(0726 은 천안으로만 생성했었다) — **이번에 원주부터** 만들어 표
  재현을 확인한 뒤 천안을 재생성한다.
- 천안 결과물에서 `868`·`366,306`·`1,949`·`799`·`938` 이 **사라졌는지 값으로 직접 확인**
  (`smoke_check` 의 leak_check 는 서술만 본다 — 표 유출은 통과시킨다, 수질 때 실증).
- ⚠️ 성상별 표 값 열 수(8·6·6·5·6)는 평면 추출 추정이다 — `blank_row` 는 셀 주소로
  걷기 때문에 열 수를 몰라도 되지만, 라벨 쓰기(`write_at`)가 옆 칸을 밀지 않는지 볼 것.

---

## ② 실결함 — 원주 수질 vars 측정값 7건이 지어낸 값이었다 (Mac 이 정정)

vars 의 pH 7.5·BOD 0.8·TOC 1.9·DO 9.2·T-P 0.021·T-N 2.451 은 **리포 어디에도 없는
값**이었다 (러프 데모 때 환각). 인풋 측정보고서 실측값(pH 7.4·BOD 2.0·TOC 8.1·SS 6.0·
DO 8.2·T-P 0.082·T-N 3.9)으로 **vars 는 정정 완료**. 측정등급도 전부 `Ia` 로 뭉개져
있던 것을 Ⅰb·Ⅵ·Ⅱ 포함 실측으로 고쳤다.

```bash
python engine/generate.py small-env water-quality 원주_무장리
python engine/smoke_check.py small-env water-quality 원주_무장리
```

재생성만 하면 된다. 끝나면 Mac 이 재채점한다.

---

## ③ 0500 환경현황 베이스 — 준비 끝, 찍으면 된다 (소환 11파트째)

spec 29토큰·slots·핸들러·vars 빌더 전부 Mac 검증 완료 (문자열 28/29 ×1 — `사업명` 은
**머리글 전용**이라 txt 검증에 안 걸린다, dry-run 에서 확인). vars 는 손으로 만들지
않는다 — 빌더가 이미 원주·천안 것을 만들어 커밋했다.

```bash
# 원본: NAS 에서 (본안) 0500 검색 → raw_data/nas/env-status/원주_무장리/ 에 hwp 복사
# 한글에서 hwpx 저장 후:
python engine/build_template.py small-env env-status
python engine/generate.py small-env env-status 원주_무장리    # 되먹임
python engine/smoke_check.py small-env env-status 원주_무장리
python engine/generate.py small-env env-status 천안_화덕리
python engine/smoke_check.py small-env env-status 천안_화덕리
```

> **09-01 갱신 (Mac)**: 5.3 통계연보 소싱이 붙었다 — `git pull` 후 원주·천안 vars 가
> 갱신돼 있고, 재생성하면 **5.3 인구·읍면·동태·주택·사업체 5표와 서술이 채워진다**
> (원주 기준 — 천안은 통계연보 미확보라 계속 비움). 캡션 지명 2건(`원주시 인구 및
> 세대추이`·`호저면 세대 및 인구`)은 Windows 가 spec 에 추가한 것을 그대로 두면 된다 —
> `{{시군}}`/`{{읍면}}` 값은 핸들러가 이미 준다.

⚠️ 표 12개 앵커·오프셋은 **전부 평면 추출 추정** — 셀 주소(`KeyIndicator`)로 실측해
맞출 것 (`env-status.slots.md` §B 표 참조). 특히:
- 대기 측정표 `PM-10` — 머리행이 두 줄이면 row_off=2
- 인구동태 `자연적 증감` — 부머리행이라 +1 해 두었다
- 읍면 표 `세대수` — **skip=1** (인구추이 표 머리에 같은 문자열이 먼저 있다)
- 5.3 표 6개는 전부 비우기다(지역개황 2.9 소싱 확장 전) — 원주 값이 남으면 안 된다

---

## ④ 채점은 Mac 이 한다 (그대로)

Windows 는 생성 + smoke_check + PDF 육안까지. 커밋 전 `git pull`.
분업: Windows = `templates/*.hwpx`·`cases/*/output.*` / Mac = `rules/`·`engine/score_*`·`golden/`.

---

## ⑤ 다음 영역 수확 ★ — Mac 에는 원본이 없다 (대여 장비 + NAS 미마운트)

①~③이 끝나면(또는 한글 대기 시간에) **다음 2주치 재료를 NAS 에서 수확**한다.
이것이 커버리지의 선행 조건이다 — Mac 은 이 수확물이 와야 distill·spec 을 진행한다.

### ⑤-1 소환 W3 어려운 파트 — 원주 + 골든 2~3건 (경량 확보, 계획 §5-4)

| 파트 | 슬러그 (naming.md 갱신됨) | 검색 |
|---|---|---|
| 입지의 타당성 (0600) | `site-suitability` | `(본안) 0600` |
| 동식물상 (0711) | `flora-fauna` | `(본안) 0711` — **대형(100p), txt 만** |
| 지형·지질 (0725) | `topo-geology` | ⚠️ 수리수문 낀 사업은 `0726` — 이름으로 찾기 |
| 경관 (0728) | `landscape` | ⚠️ 같은 이유로 `0729` 가능 |
| 부록 (0800) | `appendix` | `(본안) 0800` |
| 총량검토서 (0840) | `water-total-load` | kyj `8. 총량` 관행 (계획 §2-2 ①) |

- hwp 원본 → `raw_data/nas/{슬러그}/{사업}/` + **txt 추출 → `golden/small-env/{사업}/{슬러그}.txt` 커밋**
- ⚠️ 원본 hwp/대용량은 golden 에 동봉 금지 (txt 만 — 지역개황 때 1.4GB 함정)
- ⚠️ 파일명 `(완)` 을 믿지 말 것 — 본문 지자체명 검사 (청양 0200 이 강릉시였다)

### ⑤-2 재해 3종 표본 — W4~5 distill 의 재료 (계획 §2-2 ④⑤⑥)

- 소재평: **서식 2024.09 + 충주 지방정원 최종본 2025.02** (2건 일치 실측된 그 쌍)
- 재평(본): **재25-31 횡성 조항리 태양광** (2026.07 수정초안) — ⚠️ 표지 실물로 유형 검증 (재24-13 파일명 함정)
- 재해영향성검토: **서식 2023.09**
- `raw_data/nas/disaster_samples/` 에 확보 + **파일 목록·경로를 이 문서 아래에 추기해 커밋**.
  txt 추출도 하되 **golden 폴더에는 넣지 말 것** — 재해 카테고리 슬러그·폴더 구조는
  Mac 이 결정한다 (`paths:` 스코핑이 조용히 깨지는 함정 — 전환계획 §10-3 결정 대기).

---

## ⑤ 수확 결과 — 2026-09-01 Windows

### ⑤-1 W3 어려운 파트 (원주 무장리) — **golden 에 txt 커밋 완료**

| 슬러그 | 파트 | 글자수 | 원본(git 제외) |
|---|---|--:|---|
| `site-suitability` | 0600 입지의 타당성 | 19,321 | `raw_data/nas/site-suitability/원주_무장리/` |
| `flora-fauna` | 0711 동식물상 | 205,209 | 〃 `flora-fauna/` (73MB) |
| `topo-geology` | 0725 지형지질 | 14,558 | 〃 `topo-geology/` |
| `landscape` | 0728 경관 | 21,262 | 〃 `landscape/` (119MB) |
| `appendix` | 0800 부록 | 43,458 | 〃 `appendix/` |
| `water-total-load` | 0840 총량검토서 | 8,635 | 〃 `water-total-load/` |

본문 지자체명 검사 6/6 통과. ⚠️ 지형지질의 `충주`·`평창` 은 **원주시 인접 시군을 설명하는 정상 문장**이다 (동쪽 영월·평창, 남쪽 충주·제천) — 검사가 오탐을 낸다.

🚨 `extract.py` 결함 하나 고쳤다 — 동식물상(73MB)에 **짝 없는 서로게이트**가 있어 `write_text` 가 터지면서 **그 문서 전체를 잃었다.** `_sanitize()` 추가.

### ⑤-2 재해 3종 표본 — `raw_data/nas/disaster_samples/` (git 제외, 602MB · 39건)

txt 는 `disaster_samples/_txt/` 에 로컬 보관 — **golden 에 넣지 않았다** (재해 슬러그·폴더 구조는 Mac 결정 대기, 지시서 ⑤-2).

**소규모재해영향평가 **서식** (완본 8장)** — 14건 · 220MB · 로컬 `disaster_samples/소재평서식/`

> NAS: `/backupenv/0. 평가서/재해/2024/재24-09, 재25-07 천안시 동남구 목천읍 삼성리 소재평(240701)/2.평가서/소재평 서식/`

- `00 간지.hwp` (0.0MB)
- `00 목차.hwp` (0.0MB)
- `00 목차앞.hwp` (0.0MB)
- `00 표지.hwp` (0.0MB)
- `1장 사업 개요.hwp` (8.6MB)
- `2장 소규모 재해영향평가 대상지역 설정(완).hwp` (109.7MB)
- `3장 기초현황 조사(완).hwp` (31.4MB)
- `4장 재해영향 예측 및 평가(완).hwp` (11.7MB)
- `5장 재해영향 저감대책 수립 및 저감방안 반영(완).hwp` (17.2MB)
- `6장 유지관리계획(완).hwp` (4.1MB)
- `7장 결론(완).hwp` (0.0MB)
- `8-1 부록(완).hwp` (4.3MB)
- `8-2 부록(CD).hwp` (0.1MB)
- `8-3 부록(A3).hwp` (32.9MB)

**재해영향성**검토서** 서식 (5장)** — 7건 · 69MB · 로컬 `disaster_samples/검토서서식/`

> NAS: `〃 같은 사업 `/2.평가서/검토서 서식/``

- `1장 계획의 개요.hwp` (22.5MB)
- `2장 재해영향성검토 대상지역 설정(면적개념).hwp` (0.7MB)
- `3장 기초현황 조사.hwp` (16.2MB)
- `4장 재해유형별 위험요인 분석 및 저감방향 제안.hwp` (25.6MB)
- `5장 부록 내용 및 기타사항(계약서 추가해야함).hwp` (4.1MB)
- `간지.hwp` (0.0MB)
- `목차.hwp` (0.0MB)

**소재평 **최종본** — 재24-13 충주 지방정원 (250213 최종본)** — 14건 · 132MB · 로컬 `disaster_samples/충주최종본/`

> NAS: `/backupenv/0. 평가서/재해/2025/재24-13 충주 지방정원 조성사업 재해영향평가 용역_250207/2.평가서/250213 최종본/`

- `0. 탄금공원 지반조사보고서(토질정수).zip` (20.9MB)
- `00 간지.hwp` (0.0MB)
- `00 목차.hwp` (0.2MB)
- `00 표지.hwp` (0.3MB)
- `1장 사업 개요.hwp` (8.5MB)
- `2장 소규모 재해영향평가 대상지역 설정(완).hwp` (5.6MB)
- `3장 기초현황 조사(완).hwp` (37.3MB)
- `4장 재해영향 예측 및 평가(완).hwp` (19.0MB)
- `5장 재해영향 저감대책 수립 및 저감방안 반영(완).hwp` (9.4MB)
- `6장 유지관리계획(완).hwp` (5.5MB)
- `7장 결론(완).hwp` (0.0MB)
- `8-1 부록(완).hwp` (4.2MB)
- `8-2 부록(CD).hwp` (0.1MB)
- `8-3 부록(A3).hwp` (20.5MB)

**재해영향평가(본) — 재25-31 횡성 조항리 태양광 (수정초안 260710)** — 4건 · 181MB · 로컬 `disaster_samples/횡성재평/`

> NAS: `/backupenv/0. 평가서/재해/재25-31 횡성군 둔내면 조항리 태양광발전시설 조성사업 재평/2. 평가서/`

- `0. (본안) 0900 표지 (횡성군 조항리 태양광발전시설 조성사업).hwp` (0.0MB)
- `1-2. 재행영향평가등의 협의 사전검토 의견 회신(조항리 327번지 일원).zip` (0.3MB)
- `1. 횡성군 조항리 태양광발전시설 조성사업 재해영향평가(초안)_260520.zip` (89.1MB)
- `2. 횡성군 조항리 태양광발전시설 조성사업 재해영향평가(수정초안)_260710.zip` (91.6MB)

#### 확인·판단 필요

- ✅ **횡성 유형 검증 통과** — `0. (본안) 0900 표지` 실물이 `재해영향평가서`(재평 본안)다. 지시서가 경고한 `재24-13` 파일명 함정 없음.
- ⚠️ **서식 판이 지시서와 다르다.** 지시서는 소재평 서식 **2024.09**, 검토서 서식 **2023.09** 를 지목했는데, 확보한 것은 `재24-09 천안 목천읍 삼성리 소재평(**240701**)` 안의 서식 세트다. 서식 **개정판**을 가리킨 것인지 **사업 시점**인지 모호해 일단 완본을 받았다 — Mac 이 판단할 것.
- ★ **소재평 서식과 검토서 서식이 같은 사업 안에 함께 있다.** 두 유형 차이를 **같은 작성자·같은 시점** 기준으로 비교할 수 있어 distill 에 유리하다.
- 충주 지방정원은 `250213 최종본` 을 받았다 (지시서의 '최종본 2025.02' 와 일치).

---

## ⑥ 재채점 3건 (09-01 Mac, ①②·되먹임 반영분)

| 사업·파트 | 결과 | 분류 |
|---|---|---|
| 원주 수질 | 57.1→**60.0%** · WRONG 4 | 전부 채점기 아티팩트 (Mac 후속) |
| 원주 0726 (신규 되먹임) | 56.0% · WRONG 2 | 아티팩트 |
| 천안 0726 | WRONG 7→3 | 아티팩트 2 + **실결함 1 (아래)** |

### 🚨 실결함 잔존 — 천안 0726 지정폐기물 표에 원주 값 `11|3|1|7` 이 남았다

성상별 5표 중 4표는 비워졌는데 **마지막 지정폐기물 표만 원주 값 그대로**다. 원인 실측:
`현황조사내용` 표(조사범위 셀 `▪ 대상범위 : 천안시 및 사업계획지구`)에 **표 안 `천안시`가
하나 더 있다** — 선비우기의 skip 이 여기서 한 칸 밀려 skip=4 가 건설 표에서 끝났다.
(문서 내 표 안 `천안시` 는 6곳: 현황조사내용 + 성상별 5. 현황조사내용 표가 왜 안 훼손됐는지는
셀 주소로 실측할 것.)

**수정 방향** — 시군 라벨은 애초에 **데이터 셀이라 앵커 규약 위반**이다("덮어쓰지 않는
머리행 라벨"). 머리행 라벨로 교체:

| 표 | 앵커 | skip |
|---|---|:--:|
| 관리구역 | `행정구역(A)` | 0 |
| 생활계 / 사업장 / 건설 / 지정 | `처리방법별 발생량` | 0 / 1 / 2 / 3 |

row_off 는 부머리행 포함이라 실측(관리구역은 면적·인구 부머리 +1). 시군 라벨 쓰기는
비운 뒤 각 표에서 별도로. **신설 중인 표유출검사의 첫 검증 대상으로 이 표를 쓸 것.**

---

## ⑦ 재해 txt 커밋 경로 — Mac 결정 완료 (naming.md 갱신)

카테고리 슬러그는 기존 확정(`small-disaster`·`disaster-impact`·`disaster-review`) 그대로,
**장 슬러그를 naming.md 에 신설**했다 (1·2장은 소환과 같은 `project-overview`·`target-area`).

`disaster_samples/_txt/` 를 다음으로 커밋:

```
golden/small-disaster/천안_삼성리/{project-overview,target-area,baseline-survey,
  impact-assessment,mitigation,maintenance,conclusion,appendix}.txt   ← 소재평 서식(완) 장들
golden/small-disaster/충주_지방정원/{〃}.txt                          ← 250213 최종본
golden/disaster-review/천안_삼성리/{project-overview,target-area,baseline-survey,
  risk-analysis,appendix}.txt                                        ← 검토서 서식
```

- 표지·간지·목차·CD/A3 부록·지반조사 zip 은 커밋하지 않는다 (8-1 부록만 `appendix`)
- ⚠️ 파일명 `(완)` 검사 — 본문 지자체명이 그 사업이 맞는지 (청양 0200 전례)
- 커밋되면 Mac 이 재해 distill(구조 대조 소재평 서식↔충주 2건)에 착수한다

---

## ⑧ 표 유출 검사 ✅ 완성 (09-01 Mac) — 증거 인계 문서 §4 그대로 두 갈래

`engine/table_leak.py` 신설 + `smoke_check` ②-2 로 통합 (되먹임은 스스로 건너뜀).
지명·시설명 목록 + 뒤섞인 값(강원×새 시군 동시 출현) + 표 숫자 시퀀스 동일성.
오탐 제어: slots.md 고정값은 후속, 우선 법령·상수표 캡션 allowlist + 시퀀스 길이 문턱.
검증: 천안 7파트 — 수질·기상·0500 등 **경고 0** (오탐 없음), 그리고 아래 실유출 2건을 새로 잡았다.

### 🚨 Windows 수정 대상 (둘 다 기존 게이트 ①·①-2·② 전부 "통과"였다)

| 파트 | 유출 | 자리 |
|---|---|---|
| 천안 0100 | `원주시`·`호저면`·`무장리` 셀 3개 | 조서 표의 행정구역 열 — 지번 셀은 비웠는데 시군·읍면·리 셀이 남았다 |
| 천안 0726 | 지정폐기물 표 `11|3|1|7` | ⑥ 참조 (skip 밀림 — 머리행 라벨 앵커로 교체) |

수정 후 `python engine/smoke_check.py …` 만 돌리면 ②-2 가 재검한다.
⚠️ 검사기의 기준 지명 목록(`BASE_NAMES`)은 원주 기준이다 — 베이스가 청주인
대기질에 쓸 때는 목록을 갈라야 한다 (파일 상단 ⚠️).

---

## ⑨ 다음 배치 (급하지 않음 — 한글 켤 때 묶어서)

Mac 재해 rule 층이 완성됐다 (09-01, `rules/small-disaster/`). 아래 수확 2건이 오면
Mac 이 3종 대조·검토서 rule 로 확장한다 — 지금 시켜두면 Mac 작업과 병렬이 된다.

1. **재평(본) 표본 수확** — 재25-31 횡성 조항리 태양광 (2026.07 수정초안).
   → txt 를 `golden/disaster-impact/횡성_조항리/{슬러그}.txt` 로 (장 슬러그는 소재평과
   동일 — naming.md). ⚠️ **표지 실물로 유형 검증** (재24-13 파일명 함정 전례).
2. **재해영향성검토 표본 2건째** — 검토서 rule 은 n=2 확보 후 쓴다 (현재 횡성_옥계리 1건).
   NAS `재해` 폴더에서 검토서 완본 1건 (횡성과 다른 사업).
3. **원주 0500 재생성** — Mac 이 주거 서술 다수결 뒤집기(총가구수=일반가구수)·세대당
   계산 이동을 넣었다. `git pull` 후 생성만 (`smoke_check` 는 되먹임이라 ②-2 자동 스킵).

---

## ⑩ 재해 첫 베이스 3장 — 준비 끝 (⑨와 같은 배치로)

소재평 1·2·7장의 spec(44토큰)·핸들러·되먹임 vars(천안_삼성리, 골든 발췌)가 전부
커밋됐다. Mac 드라이런: 슬롯 43개 채움·expect 누락 0·골든 원문 일치.

```bash
# 원본: raw_data/nas/disaster_samples/소재평서식/{1장 사업 개요, 2장 …, 7장 결론}.hwp
#       → 한글에서 hwpx 저장 (파일명: 1장_사업개요.hwpx 등 — spec src 참조)
python engine/build_template.py small-disaster project-overview
python engine/build_template.py small-disaster target-area
python engine/build_template.py small-disaster conclusion
python engine/generate.py small-disaster project-overview 천안_삼성리   # 되먹임
python engine/generate.py small-disaster target-area 천안_삼성리
python engine/generate.py small-disaster conclusion 천안_삼성리
python engine/smoke_check.py small-disaster project-overview 천안_삼성리   # 나머지 두 파트도
```

- ⚠️ **되먹임 diff 예상 3글자**: `청안시`→`천안시` · `찬인시청`→`천안시청` · `3,133㎥`→`㎡`
  — 원본 오탈을 토큰이 교정한 것이다 (spec 주석). 그 외 diff 는 결함.
- ⚠️ 1장 조서 합계가 **계산 필드**면 spec `paras` 를 실측 앵커로 채워 평문화할 것 (소환 관행).
- ⚠️ 표 앵커 전부 평면 추출 추정 — `KeyIndicator` 실측으로 확정 (특히 1장 토지이용
  총괄/현황/계획의 **같은 문자열 앵커 skip**).
- ✅ `smoke_check` ②-2(표유출검사)는 **카테고리 분리 완료** (09-01 Mac) — 재해 되먹임
  (천안 삼성리)은 자동 스킵되고, 다른 사업 생성부터 천안 계열 지명·표동일을 검사한다.
- 베이스 빌드 직후 **지명 세기**(천안 삼성리 계열: 삼성리·목천읍·맹곡천·이상용) 필수 절차.

---

## ⑩ 결과 — 재해 첫 베이스 3장 ✅ (2026-09-01 Windows)

베이스 **토큰 22 + 9 + 12 = 43** (Mac 드라이런 43과 일치) · 되먹임 3장 `smoke_check` **전부 통과**.
지시서가 예고한 diff 3글자(`청안시`·`찬인시청`·`3,133㎥`) 외 실질 diff 0 — 나머지는 런 경계다.

### 앵커는 전부 틀렸다 — 추정이 하나도 안 맞았다

지시서가 "평면 추출 추정, `KeyIndicator` 실측으로 확정"이라 예고한 그대로다.
이번엔 한글을 띄우지 않고 **HWPX XML 의 `cellAddr`·`cellSpan` 을 직접 읽어** 확정했다
(셀 하나씩 `GetTextFile` 로 도는 것보다 훨씬 빠르고, 병합 폭이 숫자로 나온다).

| 자리 | 추정 | 실측 | 증상 |
|---|---|---|---|
| 토지이용 현황·계획 앵커 | `토지이용현황`/`토지이용계획` | **`구 분` + skip** (skip0 총괄·1 현황·2 계획·3 협의대상·4 사업개요) | 앵커 못 찾음 **7건** |
| 〃 데이터 시작 행 | +1 | **+2** (+1 은 `계` 행) | 합계 행이 첫 지목에 덮이고 뒤가 한 행 밀림 |
| 토지이용현황 지목 열 | 0 | **1** — `A1[1x2]='구 분'` 병합이라 데이터 행에 **빈 A열 여백** | 한 칸 왼쪽으로 밀려 구성비 칸의 기준 사업 값 `98.37`·`1.63` 이 **살아남음** |
| 조서 지번 열 | 2 | **1** (A=위치·B=지번·C=지목·D=지적·E=신청면적) | 한 칸 밀림 |
| 조서 합계 열 | 3 (`from_anchor`) | **1** — 합계 칸이 **A:C 3칸 병합** | F열(비고)까지 넘어가고 마지막 칸이라 되돌아 못 가 **덮어씀** |
| 조서 위치(A열) | (없음) | **`5x1` 세로 병합** — 값 열만 쓰면 기준 사업이 남는다 | vars `사업.조서_위치` 신설 |

> ⚠️ 여섯 자리가 **전부 CLAUDE.md §6 한 줄로 수렴한다** — 앵커에서 절대 오프셋으로 가되,
> **그 오프셋을 셀 주소로 재고 나서 쓴다.** 병합 폭을 모르면 `col_off` 를 셀 수 없다.

### 공용 층에서 나온 결함 3건 — 재해 밖에도 영향

1. **`fit_rows`·`append_rows` 에 `skip` 이 없었다.** `구 분` 이 표 5곳에 있으니 그대로 뒀으면
   **총괄 표의 행을 잘랐다.** `blank_row` 는 이미 고쳤던 자리인데 짝이 안 맞아 있었다
   (CLAUDE.md §6 ④와 같은 뿌리). `resource-cycle` 도 spec 의 `skip` 을 `fit_rows` 에
   안 넘기고 있었다 — 같이 고쳤다.
2. **`set_cell` 이 한 칸 안 줄바꿈을 못 냈다.** `\n` 만 주면 한 줄로 붙는다(`\r\n` 이어야 한다).
   재해 조서 위치 칸을 고치다 **소환 기존 결함 2건이 같이 드러났다** —
   `원주시호저면무장리`(0100 조서 위치) · `병천면 병천,가전,탑원리수신면 장산리`(지역개황 상수원보호구역).
   **골든 대조로 확인**했다 — 원주 골든 0100 은 세 줄이다. 되먹임·채점 어느 쪽도 안 잡던 자리다.
3. **`table_leak` 이 되먹임을 못 알아봤다.** 스킵 판정이 `vars.사업.시군` 만 보는데
   **재해 vars 에는 그 키가 없다**(`위치` 한 줄뿐). 자기 지명이 전부 유출로 떠 **14건 거짓 실패**.
   → `위치`·`주소_일원` 에서 기준 시군을 찾는 경로 추가 + 재해 vars 에 `시군` 채움.
   또 `지정면적(천㎡)` ⊃ `지정면` **부분문자열 덫**(인계 §3 예고)이 지역개황을 막고 있었다
   → `SUBSTRING_TRAP` 신설. 줄 전체를 낮추는 `CONTEXT_OK` 와 달리 **그 히트만** 버린다.

### 계산 필드 — `paras` 불필요

조서 합계의 `=SUM(ABOVE)` 는 핸들러가 평문을 덮어쓰자 **필드째 사라졌다**
(되먹임 diff 에서 `=SUM(ABOVE)??%g,` 4줄이 없어진 것으로 확인). 표 칸은 소환 관행대로
핸들러가 맡으면 되고, 빌더 `paras` 는 **표 밖 문단**에만 쓴다.

### 남은 것 — 7장 총괄표는 되먹임으로 못 잡는다 🚧

베이스 지명 세기 결과 **6건**: 1장 조서 위치 4건(vars 로 채움 ✅) · **7장 `맹곡천` 2건**.
7장 저감방안 총괄표는 **셀 안에 표가 또 있는 구조**라 `blank_row` 가 바깥 셀만 지운다 —
중첩표 안 기준 사업 값은 그대로 남는다. 되먹임(천안=천안)으로는 영원히 안 드러난다.
**첫 다른-사업 생성 때** 중첩표까지 도는 경로를 새로 짜야 한다 (핸들러에 실측 주석 남김).

### 회귀

소환 3파트 재생성 — 0100 원주 되먹임 · 지역개황 천안 · 0726 천안.
**diff 는 위 ②의 의도된 줄바꿈 교정 2건뿐**, 그 외 0줄. `smoke_check` 3건 전부 통과.

### PDF 육안 ④ — 9 · 6 · 4쪽

조서 표(1-2쪽)는 위치 세로 병합 두 줄·합계 `8,527`/`3,133`·비고 빈칸까지 원본 그대로.
토지이용 3표(1-5·1-6쪽)도 값·정렬 일치. 삽도는 동결이라 자리표시자다.

> 📌 **표 1-3 비고 칸의 파랑·회색 음영은 원본 서식의 것이다** — `borderFillIDRef` 순서가
> 원본과 **완전히 같다**(우리가 칠한 게 아니다). 작성자가 남긴 작업 표시로 보인다.
> 베이스에서 지울지는 Mac 판단 — 다른 사업 산출물에 그대로 나간다.
