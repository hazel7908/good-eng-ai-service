# 이름 대응표 — 한글 ↔ 영문

> **왜 이 문서가 있나**: 폴더 이름이 곧 지식 스코핑 설계이기 때문이다. rule의 `paths:` 패턴에 **글자로 박히는 이름**이 흔들리면 로드가 **에러 없이 조용히 실패**한다.
> **설계 근거**: `repo_restructure_plan.md` §2 ① · §6

---

## 표기 원칙 — 경로 패턴에 글자로 박히는 것만 영문

```
cases/small-env/*/noise-vib/**
      └───┬───┘ ┬ └───┬───┘
       카테고리  │    파트
        영문     │    영문
                사업
              = * (와일드카드) → 한글
```

| 축 | 경로 패턴에서 | 표기 | 예 |
|---|---|:--:|---|
| 인프라 폴더 | 글자로 박힘 | **영문** | `templates/` `golden/` `cases/` `engine/` `catalog/` |
| **카테고리** | 글자로 박힘 | **영문** | `small-env` |
| **파트** | 글자로 박힘 | **영문** | `noise-vib` |
| **사업** | `*` 로만 쓰임 | **한글** | `괴산_금신리` |
| 내용 전부 | (경로 아님) | **한글** | rule 본문 · 문서 · 보고서 · 변수명 |

**한 줄로**: 매칭에 실제로 쓰이는 이름만 영문.

**형식**: 소문자 + 하이픈(`kebab-case`). 밑줄·대문자·공백 금지. 단 **사업만 밑줄**(`괴산_금신리`) — 시군과 리를 가르는 구분자이고 경로 매칭에 쓰이지 않는다.

---

## 카테고리 ✅ 확정

NAS 카탈로그 290건의 `보고서종류` 필드에서 도출. 괄호 안은 실제 보유 건수.

| 한글 | 영문 | 보유 | 상태 |
|---|---|--:|:--:|
| 소규모 환경영향평가 | `small-env` | 76 | **진행 중** |
| 소규모 재해영향평가 | `small-disaster` | 55 | 미착수 |
| 재해영향평가 | `disaster-impact` | 16 | 미착수 |
| 사후환경영향조사 | `post-env-survey` | 11 | 미착수 |
| 환경영향평가 | `env-impact` | 10 | 미착수 |
| 환경보전방안검토 | `env-conservation` | 9 | 미착수 |
| 재해영향성검토 | `disaster-review` | 4 | 미착수 |
| 자연환경영향평가 | `nature-env` | 4 | 미착수 |
| 우수유출저감대책 | `runoff-reduction` | 3 | 미착수 |
| 환경성검토 | `env-review` | 3 | 미착수 |
| 비점오염원신고 | `nonpoint-pollution` | 3 | 미착수 |
| 전략환경영향평가 | `strategic-env` | 2 | 미착수 |
| 경관성검토 | `landscape-review` | 2 | 미착수 |

> 그 밖 1건짜리(배출시설허가·수질오염총량검토)는 **폴더를 만들 때 이름을 정한다.** 쓰지 않을 이름을 미리 박아둘 이유가 없다.

## 재해 3종 — 장(파트) 슬러그 ✅ 확정 (2026-09-01, W4~5 준비)

수확 표본(천안 삼성리 서식 + 충주 지방정원 최종본, 파일명 장 구성 실측) 기준.
소재평·재평(본)은 **동일 골격**(전환계획 §2-2 ⑤ 실측)이라 같은 슬러그를 쓴다.
1·2장은 소환의 0100·0300 과 공통 부품이라 **같은 이름**을 쓴다 (카테고리가 달라 경로 충돌 없음).

| 장 (소재평 `small-disaster` · 재평 `disaster-impact`) | 슬러그 |
|---|---|
| 1장 사업 개요 | `project-overview` |
| 2장 대상지역 설정 | `target-area` |
| 3장 기초현황 조사 | `baseline-survey` |
| 4장 재해영향 예측 및 평가 | `impact-assessment` |
| 5장 재해영향 저감대책 | `mitigation` |
| 6장 유지관리계획 | `maintenance` |
| 7장 결론 | `conclusion` |
| 8장 부록 | `appendix` |

| 장 (재해영향성검토 `disaster-review`) | 슬러그 |
|---|---|
| 1장 계획의 개요 | `project-overview` |
| 2장 검토 대상지역 설정 | `target-area` |
| 3장 기초현황 조사 | `baseline-survey` |
| 4장 재해유형별 위험요인 분석 및 저감방향 제안 | `risk-analysis` |
| 5장 부록 | `appendix` |

골든 배치: `golden/{카테고리}/{사업}/{슬러그}.txt` (소환과 동일 미러). 표지·간지·목차는 커밋하지 않는다.
> 카탈로그 290건 중 `보고서종류` **미분류가 120건**이다 (`catalog/review/catalog_todo.md`). 분류가 진행되면 이 표가 늘 수 있다.

---

## 파트 ✅ 확정 (2026-07-31)

**소규모 환경영향평가**의 파트 목록이다.

**근거**: 천안 화덕리 **완본 보고서의 목차** — NAS `/backupenv/2025/2. 천안시 동남구 동면
화덕리 …(완료)/2. 보고서/(본안) 0000 표지 및 목차.hwp`. 보고서 파일명에도 번호가 박혀 있어
(`0722 대기질`, `0727 소음진동`) 교차 확인된다.

| 한글 (목차 표기) | 영문 | 번호 | 쪽 |
|---|---|:--:|--:|
| 사업의 개요 | `project-overview` | **제1장** (`0100`) | 1 |
| 지역개황 | `regional-overview` | **제2장** (`0200`) | 9 |
| 대상지역의 설정 | `target-area` | **제3장** (`0300`) | 41 |
| 사업지역·주변지역 토지이용 현황 | `surrounding-land-use` | **제4장** (`0400`) | 51 |
| 환경 현황 | `env-status` | **제5장** (`0500`) | 55 |
| 동․식물상 | `flora-fauna` | **7.1.1** | 75 |
| 기    상 | `climate` | **7.2.1** | 167 |
| 대 기 질 | `air-quality` | **7.2.2** | 173 |
| 수    질 | `water-quality` | **7.2.3** | 195 |
| 토지이용 | `land-use` | **7.2.4** | 217 |
| 지형․지질 | `topo-geology` | **7.2.5** | 223 |
| 친환경적 자원순환 | `resource-cycle` | **7.2.6** | 243 |
| 소음․진동 | `noise-vib` | **7.2.7** | 251 |
| 경   관 | `landscape` | **7.2.8** | 265 |
| 입지의 타당성 | `site-suitability` | **제6장** (`0600`) | — |
| 부록 | `appendix` | **제8장** (`0800`) | — |
| 수질오염총량검토서 (별첨) | `water-total-load` | `0840` | — |

상위 묶음: `7.1 자연생태환경` / `7.2 생활환경`

> 0600·0800·0840 세 슬러그는 2026-08-31 W3 준비로 추가 (전환계획 §2-2 ① 트리 근거).
> 쪽 번호는 천안 목차 재확인 때 채운다.

> **지역개황은 제7장 바깥**이다 (제2장). 2026-08-19 골든셋 선별에서 슬러그를 정했다 —
> 절 구조는 `2.1 일반현황` ~ `2.10 종합적 지역개황` 이고, 7건 전부에서 동일했다.
> → [`20260819_지역개황_골든셋선별.md`](20260819_지역개황_골든셋선별.md)

> ⚠️ **파트 번호는 사업마다 밀릴 수 있다** (2026-08-05, NAS `(본안) NNNN` 파일명 전수 집계).
> 소음진동이 `0727` 10건 : `0728` 3건 — **`0724 수리수문` 이 끼는 사업은 뒤가 한 칸씩 밀린다**
> (수리수문 → 토지이용 0725 → 지형지질 0726 → 자원순환 0727 → 소음진동 **0728** → 경관 0729).
> 위 표는 수리수문이 없는 다수(10/13) 기준이다. **파트 번호를 문서에 쓸 때는 그 사업의 목차로 확인할 것.**
> 영문 이름(`noise-vib` 등)은 번호가 아니라 내용 기준이므로 영향 없다 — 경로 스코핑은 안전하다.

> - **`폐기물` 이라는 파트는 없다.** 해당 내용은 `7.2.6 친환경적 자원순환` 이다. 이전 추정이 틀렸다.
> - 목차의 파트 이름에는 **균등분할 공백**이 들어간다 (`기    상`, `대 기 질`, `경   관`).
>   `가. 현 황` 과 같은 종류의 표기다 — 오타가 아니다.
> - 가운뎃점은 `동․식물상`·`지형․지질`·`소음․진동` 모두 **U+2024**(ONE DOT LEADER)다.
> - 장 제목이 사업마다 다르다 — 천안 `제7장 환경현황,환경에 미치는…`(쉼표 뒤 공백 없음)
>   ↔ 원주 `제7장 환경 현황, 환경에 미치는…`. **베이스 문서 값을 함부로 통일하지 말 것.**

파트 이름은 한글로 해도 어차피 다듬어야 한다 — 목차 용어가 `소음·진동`·`동·식물상`이라 **가운뎃점이 경로에 못 들어간다.** (게다가 가운뎃점은 문서 안에서 3종류가 쓰인다 — `_category.md` §4.)

---

## 사업

**영문화하지 않는다.** 한글 그대로 `{시군}_{리}` 를 쓴다.

```
괴산_금신리    원주_무장리    여주_광대리    청양_매곡리    천안_화덕리
```

**이유**:
- 경로 패턴에서 항상 `*` 로 처리되므로 **영문화해서 얻는 게 없다**
- 290건 이상이 계속 늘고, 로마자 표기가 사람마다 갈린다 (`goesan` / `gwesan`)
- 한글 검색이 막힌다

**한글 경로 매칭은 실측으로 확인됐다** (2026-07-28, `repo_restructure_plan.md` §1 관측 5): `golden/small-env/*/noise-vib*` 패턴이 `괴산_금신리` 를 통과했다.

시군 표기는 **카탈로그의 `위치` 필드를 따른다** (`괴산군 청안면 금신리` → `괴산_금신리`). `군`/`시` 접미사는 뗀다.

---

## 새 이름을 추가할 때

1. **경로 패턴에 글자로 박히는가?** 아니오 → 한글 그대로 쓰고 이 문서에 등재하지 않는다
2. 예 → 영문 `kebab-case` 로 정하고 **이 표에 추가**
3. rule을 만든다면 `paths:` 에 그 이름이 정확히 들어갔는지 확인
4. **세션을 재시작하고** 해당 폴더의 파일을 열어 rule이 실제로 주입되는지 확인
   - 검증 전에 그 rule 파일을 직접 열면 중복 제거 때문에 **테스트가 무효**가 된다
   - 절차: `repo_restructure_plan.md` §1

## 본환·전략 — 파트 슬러그 ✅ 확정 (2026-09-03, ⑭ 수확분 Mac 확정)

Windows 가 잠정으로 지은 이름을 그대로 확정한다 — 소환과 겹치는 파트는 소환 슬러그(위 표)를 쓰고,
두 카테고리에만 있는 장은 아래. 번호는 전환계획 §2-2 목차 코드. 골든: `golden/env-impact/횡성_벨라스톤CC/`(30) ·
`golden/strategic-env/충북_수산천고명천/`(22). 표지·간지·목차·`(작성중)` 중복본은 수확 제외.

| 본환 코드 | 장 | 슬러그 | 소환 대응 |
|---|---|---|---|
| 0100 | 1장 요약문 | `summary` | — (본문 요약) |
| 0200 | 2장 사업의 개요 | `project-overview` | 소환 1장 확장 |
| 0300 | 3장 평가 대상지역의 설정 | `target-area` | 소환 3장 |
| 0400 | 4장 지역개황 | `regional-overview` | 소환 2장 축약 |
| 0500 | 5장 평가항목·범위 심의 결과 | `scoping` | ◆ 고유 |
| 0600 | 6장 주민의견 수렴 | `public-opinion` | ◆ |
| 0700 | 7장 대안설정 및 평가 | `alternatives` | ◆ |
| 0800 | 8장 환경보전목표 | `conservation-goal` | 법령표 (소환 지역개황 2.3 부류) |
| 0911·0912 | 9.1 동식물상 · 자연환경자산 | `flora-fauna` · `natural-assets`◆ | 소환 7.1.1 |
| 0921~0923 | 9.2 기상 · 대기질 · 온실가스 | `climate` · `air-quality` · `greenhouse-gas`◆ | 소환 7.2.1·7.2.2 |
| 0931 | 9.3 수질(수리·수문) | `water-quality` | 소환 7.2.3 대확장 |
| 0941~0943 | 9.4 토지이용 · 토양 · 지형지질 | `land-use` · `soil`◆ · `topo-geology` | 소환 7.2.4·7.2.5 |
| 0951~0953 | 9.5 자원순환 · 소음진동 · 위락경관 | `resource-cycle` · `noise-vib` · `landscape` | 소환 7.2.6·7.2.7·7.2.8 |
| 0961 | 9.6 인구·주거 | `population-housing` | ◆ |
| 1000 | 10장 전략환경영향평가 협의내용 반영 | `strategic-reflection` | ◆ |
| 1100 | 11장 저감방안 및 사후환경영향조사 | `mitigation-postmonitoring` | ◆ (요약장) |
| 1200 | 12장 불가피한 환경영향 | `unavoidable-impact` | ◆ (요약장) |
| 1300 | 13장 주민 생활환경·재산상 피해 | `resident-damage` | ◆ (요약장) |
| 1400 | 14장 종합평가 및 결론 | `conclusion` | ◆ |
| 1500 | 15장 부록 | `appendix-1`(인적사항) · `appendix-2`(조사목록·증빙 스캔) · `appendix-3`(측정·모델링) | 소환 8장 3분할 |
| — | 수질오염총량검토서 (별첨) | `water-total-load` | 소환 0840 |

| 전략(본안) 장 | 슬러그 | 대응 |
|---|---|---|
| 1 요약문 | `summary` | — |
| 2 개발기본계획의 개요 | `plan-overview` | ◆ (하천기본계획 개요 — 5,891줄) |
| 3 개발기본계획의 대안 | `alternatives` | 본환 7장 부류 |
| 4 대상지역의 설정 | `target-area` | 소환 3장 |
| 5 지역개황 | `regional-overview` | 소환 2장 |
| 6 환경영향평가협의회 심의 | `scoping` | 본환 5장 |
| 7 주민·관계기관 의견 | `public-opinion` | 본환 6장 |
| 8 계획의 적정성 | `plan-adequacy` | ◆ (상위계획 검토 — 검토서 3장 관련계획 부류) |
| 9 입지의 타당성 | `flora-fauna` `natural-assets` `topo-geology` `landscape` `water-quality` `hydrology`◆ `climate` `air-quality` `noise-vib` `resource-cycle` `socioeconomic`◆ | 소환 6장 검토표 + 7장 분야 현황 |
| 10 종합평가 및 결론 | `conclusion` | — |
| 11 부록 | `appendix` | 소환 8장 |
| 별첨 | `load-allocation-deferral` (오염총량 할당 유보) | ◆ |

⚠️ 전략 골든은 **하천기본계획**(수산천·고명천) 이라 태양광 소환과 사업군이 다르다 — `hydrology` 26,260줄이
그 증거(하천 수리·수문 본체). 태양광 전략은 표본이 없다.
