# 0500 환경현황 — 베이스 빈칸 명세

> 기준 사업: 원주 무장리 (`golden/small-env/원주_무장리/env-status.txt`, 552줄).
> 치환 목록은 `env-status.spec.py` (R2 외부 spec). 지식: `rules/small-env/env-status.md` · 변이표 §17.
> **요약 재구성 파트** — 계산 없음. vars 는 `engine/build_env_status_vars.py` 가
> 측정 3파트(소음진동·대기질·수질)·0100·지역개황 vars 를 **복사**해 만든다 (§승계 — 값 불일치 금지).

## A. 토큰 (29종)

| 구분 | 토큰 | 원천 |
|---|---|---|
| 5.1 서술 5 | `생태자연도_서술`(ecology.py 조립 — 도엽명은 `[확인 필요]‧번호` 로 절반 조립) · `식생_서술` `식생보전등급`(동식물상 인용) · `국토환경성_서술`(EGIS 미구축) · `산줄기_서술` | 지역개황 vars / X |
| 5.2 조사 6 | `대기_측정일시` `수질_측정일시` `소음_측정일시` · `대기_지점주소` `수질_지점주소` `소음_지점주소` | 측정 3파트 vars |
| 5.2 측정 9 | `수질_측정서술` · `소음_지점수` `소음_주간평균` `소음_야간평균` `진동_주간평균` `진동_야간평균` · `소음기준_지역` `진동기준_지역` | 〃 (기준은 nv `기준`) |
| 5.3 서술 7 | `인구_시군서술` `인구_읍면서술` `인구동태_서술` `주거_서술` `산단_서술` `산업_서술` `통계연보연도` | 지역개황 2.9 — **아직 전부 X** |
| 공통 3 | `사업명`(⚠️ **머리글 전용** — txt 추출에 잘려 검증 ×0, dry-run 필수 확인) · `시군` `읍면` | 0100·지역개황 |

## B. 표 12개 — 핸들러 (`engine/parts/small-env/env-status.py`)

| 표 | 처리 | 앵커 (⚠️ 전부 Windows 실측 전) |
|---|---|---|
| 생태자연도 (면적·구성비) | 비움 (등급분포 미산출) | `1등급권역` |
| 식생 보전등급 (10행) | **라벨까지 비움** — 행 구성이 사업 고유 | `해 당 식 생 형` |
| 생태계 현황 (5행) | 종수·법정보호종 비움 | `분류군종수` |
| 대기 측정결과 | A-1 행 6값 채움 | `PM-10` (머리 두 줄이면 row_off=2 ⚠️) |
| 수질 측정결과 (8행) | 행마다 라벨 앵커 재탐색 · 값+등급 | `pH` `BOD (㎎/L)` … |
| 소음 / 진동 | 데이터 행 + 기준값 2칸 | `측정횟수` / `주 간(dB(V))` |
| 5.3 인구추이·동태·주택·산단 | 비움 (연도 열 포함 — 판 가변) | `세대당 인구(명)` `자연적 증감`(부머리행 +1) `주택보급률(%)` `분양상태` |
| 5.3 읍면·사업체 | 라벨 칸이 데이터인 표 — 읍면명·시군만 쓰고 값 비움 | `세대수`(skip=1) `회사이외 법인` |

## C. 미결 (실무자·실측)

- 조사지점 표 지점명 `A - 1` `W - 1` `N·V- 1` — 표기 4종 변이라 **베이스 유지** ([실무자 확인]). 측정표 안 지점명(`A-1` `N - 1` `V - 1`)도 동일.
- 대기 서술("전 항목이 대기환경 기준치 이내를 만족")·수질 등급 만족 여부 — 판단 서술이라 베이스 유지, 상회 사업 미관측.
- 산단 표 행 수 12는 원주 고유 — 사업마다 다르다. `fit_rows` 도입은 실측 후.

## D. 검증 이력

- 문자열 검증: 28/29 ×1 이상 (사업명만 머리글 ×0 — 추출 잘림), `자) 통계연보` ×5. (2026-08-31 Mac)
- vars 빌더 + slots 드라이런: 원주 7/29 채움 · 천안 12/29 채움, expect 누락 0. (〃)
