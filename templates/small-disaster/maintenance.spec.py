# -*- coding: utf-8 -*-
"""소재평 6장 유지관리계획 베이스 명세 — 기준 사업: 천안_삼성리 서식 세트.

**반고정 뭉치의 실측 (09-03)**: 천안↔충주 본체 380줄을 diff 하니 차이 62줄이 **전부 글머리
기호(`⚫`·`∙`·`◦`)와 `제 N 단계 :` 콜론**이었다 — 내용은 2/2 완전 고정. 사업 고유 자리는
**유지관리대장 작성 항목 표의 시설물 목록 4셀**(5장 시설 제원 승계)뿐이고 그건 핸들러 몫이다.
🔬 **토큰은 0개다** (2026-09-03 Windows 실측). 머리글에 사업명이 있을 것으로 봤으나
머리글은 `제6장 유지관리계획` **뿐이고**, 문서 전체(573줄)에 사업명·지명·시행자가
**하나도 없다** (`근린생활`·`주기장`·`조성사업`·`천안`·`목천`·`삼성리` 전부 0회 —
XML 머리글/바닥글 직독으로도 0). 소재평 7장 중 **유일하게 빈칸이 필요 없는 장**이다.
사업 고유는 표 4셀뿐이고 그건 핸들러가 채운다. 행정사항 표·서술은 법령 고정.
지식: rules/small-disaster/maintenance.md.
"""

MAINTENANCE_REPLACE = []      # 뚫을 자리가 없다 (위 실측)

SPEC = {
    "source": "천안_삼성리",
    "src": "raw_data/nas/disaster_samples/소재평서식/6장_유지관리계획.hwpx",   # hwp → hwpx 변환 후
    "replace": MAINTENANCE_REPLACE,
    "paras": [],
    "cells": [],
    # 🖼 이 장의 그림 9장은 **법령 별지 서식 스캔**이다 (협의내용 관리대장·착공/준공
    #    통보서·급경사지 관리 실무편람). 사업 고유 사진이 하나도 없어 걷어내면 안 된다 —
    #    기본 동작대로 두면 3.4KB 자리표시자로 바뀌어 **9쪽이 빈 상자**로 나간다 (09-03).
    "keep_figures": True,   # 대장 작성 항목 표 4셀은 핸들러 (5장 vars 승계 · 없으면 비움)
    "expect": [],
}
