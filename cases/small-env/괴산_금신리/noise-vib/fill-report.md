# 채움 내역 — 괴산_금신리 / noise-vib

> 생성 데이터: `cases/small-env/괴산_금신리/vars/noise-vib.json` (작성일 2026-07-29) · 근거 규칙: `.claude/rules/small-env/noise-vib.md`
> 이 문서는 vars 에서 기계적으로 생성된다 (`engine/fill_report.py`). 내용을 고치려면 vars 를 고치고 다시 뽑을 것.

검토 요약: **직접 채울 것 4건 · 확인할 것 4건 · 자동 채움 36칸**

## ❓ 실무자가 채워야 하는 것 (4건)

문서에는 `[확인 필요]` 또는 `-` 로 나간 자리다. 값을 지어내지 않았다.
건수는 **사실(vars 항목) 기준**이라 아래 ✅ 빈칸 수와 1:1이 아니다 — 본문 빈칸으로 가는 항목은 ✅ 목록에 `← ❓` 로 표시되고, **표 안 자리로 가는 항목은 ✅ 목록에 나타나지 않는다.**

| 항목 | 왜 비웠나 |
|---|---|
| 현황.조사시기 | 인풋에 없다. 측정일시(09.18~19)와 다른 값이며 실무자만 안다 |
| 현황.측정지점.비고 | 시설명. 인풋에 없다 (PoC 의 '이서건설' 은 출처 불명) |
| 예측.지점[*].비고 | 시설명은 현장조사 데이터 |
| 예측.일작업량_㎥ | 설계도서 필요 |

## ⚠️ 채웠지만 확인이 필요한 것 (4건)

삽도 판독(정밀도 낮음)이거나, 회사 표준이 없어 기본값을 쓴 항목이다.

| 항목 | 분류 | 근거와 한계 |
|---|:--:|---|
| 현황.측정지점.이격거리_m | 삽도△ | 삽도 판독. 정밀도 낮음 |
| 예측.지점[*] | 삽도△ | 이름·방향·이격거리 전부 삽도 판독. 2차 PoC 오류 61건 중 10건이 이 원인 |
| 저감.목표기준_지역문자 | 판단 | 골든셋 4:1 로 갈린다. 기본값 '가' |
| 저감.분산투입_감산량 | 판단 | 작성자 선택. 골든셋 1.7 / 3.3 / 4.9 |

## ✅ 자동으로 채운 빈칸 (36칸)

베이스 문서의 `{{토큰}}` 자리에 들어간 값. 빈칸 위치는 `templates/small-env/noise-vib.slots.md` 참조.

| 빈칸(토큰) | 들어간 값 |
|---|---|
| `사업명` | 괴산군 청안면 금신리 153번지 일원 태양광발전시설 조성사업 |
| `조사시기` | [확인 필요] ← ❓ |
| `측정일시` | 2025년 09월 18일 ~ 09월 19일 |
| `측정지점_주소` | 충청북도 괴산군 청안면 질마로불당재길 48-56 |
| `측정지점_이격거리` | 25 |
| `측정지점_비고` | [확인 필요] ← ❓ |
| `소음_주간평균` | 49.0 |
| `소음_야간평균` | 44.0 |
| `진동_주간평균` | 15.0 |
| `진동_심야평균` | 10.0 |
| `소음환경기준_지역` | 다 |
| `소음환경기준_주간` | 65 |
| `소음환경기준_야간` | 55 |
| `생활진동규제_지역` | 나. 그밖의 지역 |
| `생활진동규제_주간` | 70 |
| `생활진동규제_심야` | 65 |
| `측정결과_도입` | 사업계획지구 주변 1개 지점의 소음 측정 결과 |
| `측정지점도_캡션` | 소음․진동 측정지점도 |
| `진동측정결과_도입` | 사업계획지구 주변 1개 지점의 진동 측정 결과 |
| `현황_자료유형` | 측정자료 |
| `현황_소제목_접미` |  |
| `측정지점_도입` | 본 사업시행으로 인하여 직·간접적인 영향이 예상되는 지역 중 사업계획지구와 가장 인접한 1개 지점을 선정하여 소음·진동 측정을 실시하였다. |
| `예측소음도_결과서술` | 전 지점에서 기준치를 만족하는 것으로 예측되었다 |
| `예측지점_수` | 8 |
| `최인접_이격거리` | 150 |
| `공종` | 토공 |
| `일작업량` | 190 |
| `시간당작업량` | 23.75 |
| `저감1_접두` |  |
| `소음영향_서술` | 있을 |
| `진동영향_서술` | 없을 |
| `목표기준_지역문자` | 가 |
| `목표소음_주거` | 65 |
| `목표소음_축사` | 60 |
| `목표진동_주거` | 65 |
| `목표진동_축사` | 57 |

> 표 안의 계산값(합성 레벨·거리 감쇠·저감 체인 등)은 이 목록에 없다 — `engine/calc.py` 가 계산하며 골든셋 대조 자체검증을 내장한다. 골든셋이 있는 사업의 항목별 채점은 같은 폴더 `validation.md`.
