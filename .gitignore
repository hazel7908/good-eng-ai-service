# Python
.venv/
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store
Thumbs.db

# Raw data (HWP, PDF, JPG originals - too large for git)
raw_data/
소규모환경 태양광사업 관련 자료_260219/

# Extracted texts (intermediate, regenerable from raw_data)
extracted_texts/

# IDE
.idea/
.vscode/
*.swp
.claude/worktrees/clever-blackburn/

# 전국 통계 값 본체 (재생성 가능 — build_stats_values.py). 판 지문은 매니페스트에 커밋된다
catalog/data/stats_values/

# KRF 원자료 (87MB · 재취득 가능 — reachfile.py --download)
raw_data/nas/stats/_national/KRF/
catalog/review/.law_api_cache/

# 🚨 사업자·소유자 **명단** 파일은 커밋하지 않는다 (2026-09-08).
#    횡성 재평  에 주민등록번호 3·자택주소·연락처가 있었다.
#    사업 정보가 아니라 개인정보 대장이라 가릴 것이 아니라 **안 받는 것**이 맞다.
#    (신청서 본문은 pii_check --fix 로 가려서 쓴다 — 거긴 사업 정보가 섞여 있다.)
*명단*.xlsx
*명단*.xls
