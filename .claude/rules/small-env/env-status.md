---
paths:
  - "cases/small-env/*/env-status/**"
  - "cases/small-env/*/vars/env-status*"
  - "templates/small-env/env-status*"
  - "golden/small-env/*/env-status*"
---

# 환경 현황 (제5장 0500) — 파트 지식

> 골든셋 7건 (08-31). 변이표 `_variants.md` §17. 10쪽 — **요약 재구성 파트**.

## ①구조 (7/7) 5.1 자연생태 5절 / 5.2 생활환경(환경질 3종) / 5.3 사회·경제 3절

## ②변수→출처 — 신규 값이 거의 없다
- 5.1.1 생태·자연도: **지역개황 ecology.py 등급 판정 재사용** (8/8 검증된 그것)
- 5.1.2 식생보전 등급도: 동식물상(0711) 산출 인용 — 인풋 관행 확인 대상 (§7 클라이언트 질문 ②)
- 5.1.3 국토환경성평가등급: EGIS — 신규 소스 (지역개황 때 "EGIS 불필요" 판정은 삽도 얘기 — 등급 조회는 별건)
- 5.1.5 산줄기: 산경표 서술 — [확인 필요] 후보
- 5.2 환경질: **기상·대기질·수질·소음진동 vars 의 측정값 요약** — 원 파트와 값 불일치 금지 ★
- 5.3 인구·주거·산업: **지역개황 2.9 와 동일 원천** (통계연보)

## ③계산 없음 (요약 문장 생성뿐) ④⑤ 분기 미관측 — spec 작성 때 7건 문장 대조로 확정

## ⑥오류 측정 파트와 이 파트의 값이 어긋나면 그것이 결함 — 생성은 같은 vars 에서 뽑는다

## §승계 — 측정 3파트 vars 규약 (2026-08-31 확정, 원주 5.2 실측)

5.2 가 인용하는 것은 정확히 셋이다: **조사일시 3줄 · 조사지점 표 3행(지점명+주소) ·
측정결과 표(대기 항목값 / 수질 항목값 / 소음 주·야간값)**. 전부 소음진동·대기질·수질
vars 에 이미 있는 값이다.

> **규약: env-status.json 은 손으로 만들지 않는다.** vars 빌더(후속,
> `engine/build_env_status_vars.py`)가 같은 사업의 `vars/noise-vib.json` ·
> `air-quality.json` · `water-quality.json` 에서 키를 **복사**해 만든다.
> "값 불일치 금지"의 구현 = 복사 원천 단일화. 세 파일 중 없는 것이 있으면
> 그 절 값은 `[확인 필요]` 로 두고 `_확인필요` 에 남긴다 (러프 원칙 그대로).
> 5.1(생태자연도=ecology.py)·5.3(지역개황 값 저장소)도 같은 빌더가 채운다.

## ⑦변이표 → §17. spec 29토큰·핸들러·빌더 ✅ 08-31 Mac (문자열 검증·드라이런 완료) — Windows 베이스 대기 (`docs/20260831_윈도우_0500_결함수정.md` ③)
