#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""env-impact `regional-overview` 핸들러 — C 틀 (2026-09-04 Mac). 규약: vars `slots`. 지역개황(소환 2장 축약) — 개황·지목·용도·보호지역·기초시설 서술 전부 토큰(지역개황 정형 문장 — 소환 `build_vars_regional` 이식 1순위 D2) · 종합표 ○×·통계 표는 비움 · 골프장 입지기준 발췌(241~290)는 반고정.

B 단계: 소환 `build_vars_regional` 절 조합 일반화(D2) — 서술·표를 통계 소싱으로 채운다.
"""
from hwp_util import MISSING, blank_tables

BLANK = [("구성비", 2, 3), ("해당유무", 1, 2), ("포장율(%)", 1, 1), ("배출시설", 2, 1),
         # ⚠️ 09-07 Mac 잔존 전수 — 앵커는 베이스 추출로 유일성 확인, Windows 실측 검증 대상
         ('증설부지 기준', 2, 1), ('제외한 전역', 1, 1), ('해당여부', 1, 2),
         # ⚠️ 09-07 실측 추가 — 지리적 위치·보호구역·취수장 표에 기준 사업 지명이 남았다.
         ("연장거리", 2, 1), ("보호구역명", 1, 1), ("지정연도", 1, 1), ("공급정수장", 1, 1),
         ("유입하수량", 2, 1), ("총매립용량", 1, 1), ("소각방식", 1, 1), ("조성현황", 1, 1), ("청 정", 1, 1),
         # ⚠️ 09-08 — 되먹임 사각 7표(사업 고유). 산출물에서 머리 셀 실측.
         #    저황유 표는 **법령 별표**라 뺐다(값은 전국 공통 — 다만 `제천시` 볼드 강조가
         #    기준 사업 고유다. 서식은 텍스트에 안 남아 bold_check 몫).
         ("비도시지역", 2, 1), ("도시자연공원", 2, 1), ("소음･진동", 2, 1),
         ("국가지정문화유산", 2, 1), ("정수처리", 2, 1), ("계획대상지", 2, 1),
         # ⚠️ 09-08 3차 — 정수장만. 저황유는 **법령 별표**라 고정(값은 전국 공통 —
         #    다만 `제천시` 볼드 강조가 기준 사업 고유다. 서식은 bold_check 몫).
         ("정수처리", 2, 1)]


def build_slots(v):
    s = v.get("slots") or {}
    return {k: (s.get(k) if s.get(k) not in (None, "") else MISSING) for k in EXPECT}


def build_tables(hwp, v):
    for anchor, hdr, limit in BLANK:
        k = blank_tables(hwp, anchor, hdr, limit)
        if k == 0:
            print(f"    WARNING: 앵커 '{anchor}' 못 찾음 — 기준 사업 값 잔존 위험")


EXPECT = ['사업지_읍면', '청정연료_서술', '읍면개황_서술', '시군개황_서술', '배출허용_서술', '자연공원_서술', '지목_서술', '개황718_서술', '대기특별_서술', '대기권역_서술', '야생생물_서술', '도로_서술', '상수원_서술', '개황588_서술', '개황484_서술', '습지_서술', '생태경관_서술', '백두대간_서술', '수변_서술', '개황715_서술', '종합개황_도입', '산림유전_서술', '용도지역_서술', '사업명', '시군']
