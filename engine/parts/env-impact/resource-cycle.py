#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""env-impact `resource-cycle` 핸들러 — C 틀 (2026-09-04 Mac). 규약: vars `slots`. 자원순환 — 현황 서술 9(시군 통계 — 소환 0726 rule·`calc_waste` 이식 자리)·예측 서술 9·값 2 = 22토큰 · 통계 표 9와 산정 표(폐유·인부·건폐·임목·농약·비료·슬러지)는 비움 · 저감방안(분리수거 요령 등)은 반고정. BLANK 앵커는 Windows 실측 전 추정.

B 단계: 현황 통계는 소환 0726 소싱(`전국 폐기물 발생 및 처리현황` — `stats_registry`)·산정은 `calc_waste.py` 이식.
"""
from hwp_util import MISSING, blank_tables

BLANK = [("재활용", 2, 4), ("총매립용량", 1, 1), ("소각방식", 1, 1),
         ("인원산정식", 2, 1), ("훼손수목", 2, 2), ("성분량", 2, 2),
         # ⚠️ 09-07 실측 추가 — 첫 되먹임에서 **표 주소가 그대로 남았다**.
         #    `{{시군}} 횡성읍 학오로 89` 처럼 토큰과 기준 사업 지명이 한 셀에 섞여 있어
         #    다른 사업이면 `천안시 횡성읍` 이 된다 — 남의 값보다 나쁜 뒤섞인 값이다.
         ("유입하수량", 2, 1), ("처리방법", 1, 4), ("공공/민간", 1, 1), ("주소지", 1, 1),
         # ⚠️ 09-08 — 되먹임 사각 15표 중 사업 고유 10개(산출물에서 머리 셀 실측).
         #    뺀 넷은 고정이다: 건설폐기물 **발생원단위**(전국 상수) · 슬러지 **계산식** ·
         #    지정폐기물 보관시설 **표준 사양** · 사후환경영향조사 **표준 문안**.
         ("청소구역(B)", 2, 1), ("잡재료", 2, 1), ("투인인원(인)", 1, 1),
         ("건물1", 1, 1), ("2020년", 1, 1), ("1홀당 이용객(인/홀)", 1, 1),
         ("이용객(인/년)", 1, 1), ("발생시기 및 주기", 2, 1),
         ("ha당 사용량 (kg/ha·년)", 2, 1), ("ss 농도", 2, 1),
         # ⚠️ 09-08 3차 — 남은 6표 중 사업 고유 2개. 뺀 넷은 고정이다:
         #    건설폐기물 **발생원단위**(전국 상수) · 슬러지 **계산식** ·
         #    지정폐기물 보관시설 **표준 사양** · 사후환경영향조사 **표준 문안**.
         ("농약사용면적(㎡)", 2, 1), ("발생 예정", 2, 1)]

def build_slots(v):
    s = v.get("slots") or {}
    return {k: (s.get(k) if s.get(k) not in (None, "") else MISSING) for k in EXPECT}


def build_tables(hwp, v):
    for anchor, hdr, limit in BLANK:
        k = blank_tables(hwp, anchor, hdr, limit)
        if k == 0:
            print(f"    WARNING: 앵커 '{anchor}' 못 찾음 — 기준 사업 값 잔존 위험")


EXPECT = ['골프장명', '골프장명_약칭', '관리구역_서술', '인부_서술', '훼손수목_서술', '슬러지SS_서술', '하수처리_서술', '농약원단위_서술', '슬러지_서술', '농약사용면적_주', '건설폐기물_서술', '폐농약_서술', '음식물_서술', '매립_서술', '소각_서술', '인부폐기물_서술', '생활폐기물_서술', '사업장폐기물_서술', '운영폐기물_서술', '폐유예측_서술', '지정폐기물_서술', '이식수목_주수', '사업명', '시군']
