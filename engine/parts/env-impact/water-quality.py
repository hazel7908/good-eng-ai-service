#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""env-impact `water-quality` 핸들러 — C 틀 (2026-09-04 Mac). 규약: vars `slots`. 수질(수리·수문 대확장 12,463줄) — 사업 고유 서술 자동 수확(지명·수계 낀 결과 문장 ≤22) · 측정·부하·우수유출·저류지·관개 표 비움(calc_water 확장은 B — 소환 0723 rule) · 이론·기준표 반고정. BLANK 앵커는 Windows 실측 전 추정."""
from hwp_util import MISSING, blank_tables

# ⚠️ 09-07 실측 — 하천일람 머리행은 **1행**이다 · `하천연장` 은 이 문서에 없다(중권역 표로 대체).
BLANK = [("유로연장", 1, 3), ("중권역코드", 2, 2), ("수질측정", 2, 4), ("BOD", 2, 8), ("먹는물", 2, 3), ("ha/year)", 2, 4), ("유출계수", 2, 4), ("강우강도", 2, 3), ("관개용수", 2, 2),
         # ⚠️ 09-07 PDF 육안 ② 적발 — 상수도·하수도 표가 횡성 통계 그대로(되먹임 사각 목록
         #    docs/20260907_손대지않은표.md). 머리 셀이 줄바꿈 분할이라 붙은 조각으로 잡는다.
         ("급속여과", 1, 1), ("급수전수", 1, 1), ("하수도보급률", 2, 1),
         # ⚠️ 09-08 — 되먹임 사각 41표 중 사업 고유 20여 개. 머리 셀을 산출물에서 실측했다
         #    (베이스와 색인으로 짝지으면 안 된다 — 중첩표 때문에 145 : 191 로 개수가 다르다).
         #    법령 조문 인용(지하수 사후관리)·설계 절차 순서도는 고정이라 뺐다.
         ("지정면적(k㎡)", 1, 1), ("수질(폐수)", 2, 1), ("지정(변경)사유", 2, 1),
         ("생활용", 1, 1), ("측정소명", 1, 1), ("지 점 명", 2, 3), ("조사시기", 1, 2),
         ("지점번호", 2, 2), ("시간비율", 2, 1), ("토 양 형", 2, 2),
         ("유출곡선지수(CN)", 2, 1), ("통과(누가)", 2, 1), ("VM값", 2, 1),
         ("2020년", 1, 1), ("1홀당 이용객(인/홀)", 1, 1), ("연간 이용객(인)", 2, 1),
         ("기 운영시", 2, 2), ("인원 산정기준", 2, 2), ("농약사용면적", 2, 2),
         ("개발전 유출량", 2, 2), ("방류수 수질기준", 1, 1), ("방류관제원", 2, 1),
         ("유출량차이", 2, 1), ("면적 × 유출계수", 1, 1), ("초기우수량", 2, 1),
         ("최초협의시(18홀)", 2, 1)]


def build_slots(v):
    s = v.get("slots") or {}
    return {k: (s.get(k) if s.get(k) not in (None, "") else MISSING) for k in EXPECT}


def build_tables(hwp, v):
    for anchor, hdr, limit in BLANK:
        k = blank_tables(hwp, anchor, hdr, limit)
        if k == 0:
            print(f"    WARNING: 앵커 '{anchor}' 못 찾음 — 기준 사업 값 잔존 위험")


EXPECT = ['골프장명', '골프장명_약칭', '중권역_서술', '단위유역_서술', '수질측정망_범위', '목표기준_서술', '수질149_서술', '수질11897_서술', '수질9455_서술', '수질379_서술', '수질75_서술', '수질11410_서술', '수질113_서술', '수질133_서술', '수질8522_서술', '수질20_서술', '수질147_서술', '사업명', '읍면리', '시군']
