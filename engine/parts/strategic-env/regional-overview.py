#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""strategic-env `regional-overview` 핸들러 — C 틀 (2026-09-04 Mac). 규약: vars `slots`. 지역개황(소환 2장) — 정형 서술 수확 + 하천명 · 종합표·통계 표 비움(B: build_vars_regional 이식). BLANK 앵커는 Windows 실측 전 추정."""
from hwp_util import MISSING, blank_tables

BLANK = [("구성비", 2, 4), ("해당유무", 1, 2), ("배출시설", 2, 2), ("포장", 2, 2),
         ("연장거리", 2, 1), ("백두대간보호지역", 2, 1), ("지정연도(년)", 1, 1),
         ("수종", 1, 1), ("청 정", 1, 1), ("단지명", 1, 1), ("운영주체", 1, 1),
         ("유로연장", 1, 2),
         # ⚠️ 09-08 anchor_suggest — 되먹임 사각 15표 중 지역값 12개. 캡션(`배출시설`
         #    `자연공원 지정현황`)으로는 못 잡아 머리행 셀로 바꿔 달았다.
         #    저황유 공급지역은 **법령 별표**(전국 공통)라 뺐다 — 소환과 같은 판정.
         ("시·군·구별 면적(㎢)", 2, 1), ("국가지정문화재", 2, 1), ("공급정수장", 1, 1),
         ("급수지역", 1, 1), ("대기(가스․먼지․매연 및 악취)", 2, 1), ("승용차", 1, 1),
         ("처리공법", 1, 1), ("총매립용량", 1, 1), ("소각방법", 1, 1),
         ("재활용 방법", 1, 1), ("정온시설", 2, 3)]


def build_slots(v):
    s = v.get("slots") or {}
    return {k: (s.get(k) if s.get(k) not in (None, "") else MISSING) for k in EXPECT}


def build_tables(hwp, v):
    for anchor, hdr, limit in BLANK:
        k = blank_tables(hwp, anchor, hdr, limit)
        if k == 0:
            print(f"    WARNING: 앵커 '{anchor}' 못 찾음 — 기준 사업 값 잔존 위험")


EXPECT = ['시군위치_서술', '계획명', '시군', '서술_43', '서술_44', '읍면', '서술_101', '서술_181', '서술_183', '서술_185', '서술_187', '서술_189', '서술_228', '서술_269', '서술_271', '서술_286', '서술_308', '서술_310', '서술_318', '서술_357', '서술_371', '서술_373', '서술_377', '서술_439', '서술_473', '서술_515', '서술_550', '서술_597', '서술_617', '서술_667', '서술_691', '서술_729', '하천2_명', '하천1_명', '서술_927']
