# 환경영향평가 보고서 AI 자동화 프로젝트

소규모 환경영향평가 보고서(태양광 발전시설)의 파트별 초안을 AI로 자동 생성한다.
**베이스 문서(빈칸 뚫린 HWPX) + 변수 명세 + 신규 사업 인풋 → 생성.**
기술: Windows + 한글 프로그램 API (win32com) + Python 3.10+

> 이 파일은 **항상 로드**된다. 짧게 유지할 것 — 분량이 늘면 지시 준수율이 떨어진다.
> 구조·PoC 결과·문서 목록은 [`README.md`](README.md), 설계 근거는 [`docs/knowledge_architecture.md`](docs/knowledge_architecture.md).

---

## 폴더 3분할 — 이것만은 헷갈리면 안 됨

| 폴더 | 정체 | 생성 중 접근 |
|---|---|:--:|
| `templates/` | 베이스 문서 (빈칸 뚫린 시작 파일) | ✅ |
| `golden/` | **골든셋 = 완성된 정답 보고서** | ❌ **금지** |
| `cases/{카테고리}/{사업}/` | 사업별 입력(`input/`) · 변수(`vars/`) · 생성물(`{파트}/`) | ✅ |

`engine/` 생성 엔진 · `catalog/` NAS 카탈로그 · `_archive/poc-2026-03/` 옛 PoC(**참조 금지**)

---

## 핵심 원칙

### 1. 정답지 참조 금지

`golden/` 은 **검증 단계에서만** 연다. 실무에는 정답이 없다. 생성은 인풋(사업개요 + 환경질측정 보고서)만으로 한다.

### 2. 환각 금지 — 없으면 `[확인 필요]`

인풋에 없는 수치·지점명·파트 번호를 **만들어내지 않는다.** 부족하면 `[확인 필요]` / `[현장조사 필요]` 로 표시한다.

### 3. 원본 텍스트 우선

변수 값은 **텍스트 파일에서 직접 확인**한다. 삽도(JPG) 추출값은 부정확하다 — 2차 PoC 오류 61건 중 **10건이 삽도 추출 부정확**이었다.

### 4. 기준 체계 혼동 주의 ⚠️

소음·진동 기준이 **3가지 서로 다른 체계**로 나뉜다(소음환경기준 "가"~"라" / 생활소음규제 가·나 / 생활진동규제 가·나). 혼동이 2차 PoC의 Critical 오류 1번이었다. → `.claude/rules/common.md`

### 5. 규칙에는 근거 건수를 적는다 `(n/5)`

파트 규칙은 골든셋에서 도출한다. **한 사업에서만 관측된 것을 법칙으로 쓰지 않는다.** `(1/5)` 항목은 그 사업의 우연일 수 있다.

> 실제로 2026-07-29 재검증에서 **괴산 1건에서 일반화된 규칙 3개가 틀린 것**으로 드러났다 (주거 진동 기준·목표기준 지역 문자·저감량 계산). → `golden/small-env/_variants.md`

---

## 지식은 경로로 로드된다

파일을 **여는 행위 자체**가 트리거다. Claude의 판단·기억이 개입하지 않는다.

| rule | 로드 시점 |
|---|---|
| `rules/common.md` | 항상 (`paths:` 없음) |
| `rules/small-env/_category.md` | `cases/`·`templates/`·`golden/` 의 `small-env/` |
| `rules/small-env/noise-vib.md` | 그중 소음진동 파트 |
| `rules/hwpx.md` | `engine/` · `templates/` |

⚠️ **`paths:` 가 틀리면 에러 없이 조용히 로드가 안 된다.** 폴더·파일 이름을 바꾸면 반드시 재검증 → [`docs/repo_restructure_plan.md`](docs/repo_restructure_plan.md) §1, 이름 규칙은 [`docs/naming.md`](docs/naming.md)

---

## 현재 상태

> 갱신일 2026-07-31. **파이프라인이 처음으로 끝까지 돌았다.** 1~8단계 전부 완료.
> 상세 이력은 `git log` + `docs/repo_restructure_plan.md` §9.

| 단계 | 내용 | 상태 |
|:--:|---|:--:|
| 0~3.5 | 구조 개편 + 경로 스코핑 검증 | ✅ |
| 4 · 4.5 | 파트 지식 7장르 재편집 · 카테고리층 · 스킬 정리 | ✅ |
| 5 | 사업 데이터·계산 분리 (`vars/` + `calc.py`) | ✅ |
| 7 · 8 | `distill-golden` 스킬 · 문서 동기화 | ✅ |
| **6** | **원주 골든셋 → 베이스 문서(빈칸 뚫기)** | ✅ 2026-07-31 |

베이스 문서 `templates/small-env/noise-vib.hwpx` (토큰 25종). 수작업 대신 **`engine/build_template.py` 로 재현 가능**하다 — 가운뎃점 3종·스마트따옴표를 손으로 치면 매칭이 깨진다.

`engine/generate.py` 는 win32com 으로 **실행 검증됨** (괴산 생성 성공, 빈칸 잔여 0).

### 측정 결과 — 44.3% → **50.8%**

`cases/small-env/괴산_금신리/noise-vib/validation.md` (2차 PoC 61개 항목 재채점).
삽도 JPG를 넣으면 실질 54.1%. 기대치 60%에는 못 미쳤다.

> ⚠️ **괴산으로는 규칙 개선을 측정할 수 없다.**
> R1·R2 는 "괴산 1건에서 일반화된 규칙이 틀렸다"고 판정해 다수(4/5)로 고친 것인데,
> 재측정 대상이 바로 그 괴산이다. **규칙을 옳게 고칠수록 괴산 점수는 떨어진다** (실제로 2건 후퇴).
> **다음 측정은 여주·천안·청양으로 할 것.**

### 다음 할 일

| # | 내용 | 근거 |
|:-:|---|---|
| 1 | 다른 사업(여주·천안)으로 재측정 | 위 ⚠️ |
| 2 | 예측결과 서술 빈칸 추가 (`P-1 지점을 제외한` 잔존) | `noise-vib.slots.md` A-2 |
| 3 | 표 29 이격거리 열 사업별 선택 | `noise-vib.md` §3-3 |
| 4 | 시간당 작업량 = 일 작업량 ÷ 8 | 〃 §6-2-1 |
| 5 | 삽도 JPG 확보 후 재실행 (`--raw-dir`) | UNAVAIL 2건 |

### 판단 대기 — 실무 지식이 필요한 것

| 항목 | 상태 | 기록 위치 |
|---|---|---|
| 파트 목록·번호 | `noise-vib` 7.2.7 · `air-quality` 7.2.2 만 확정. **완본 보고서 1건 필요** | `docs/naming.md` |
| 상회 0건일 때 최종 저감대책 표 | 괴산은 넣고 여주·천안은 안 넣음. 규칙 못 찾음 | `noise-vib.md` §4-1 |
| 분산투입 감산량 (1.7~4.9) | 작성자 판단. 기본값 1.7 | `noise-vib.md` §3-3 |
| 주거 진동 기준 65 vs 70 | 골든셋 4:1 → 65 채택 | `noise-vib.md` §2-5 |

---

## 코드 작성 시

- Python 3.10+, 가상환경 `.venv/`
- 한글 API 사용 시 반드시 `.claude/rules/hwpx.md` 참조
- **엔진은 파트를 몰라야 한다** — 파트 고유 정보는 코드가 아니라 `cases/*/vars/` 와 rule에 둔다
- 원본 HWP/PDF는 `raw_data/` (git 제외)
- Python XML 직접 조작 방식은 **파일 무결성 문제로 중단** (한글 API만 사용)
