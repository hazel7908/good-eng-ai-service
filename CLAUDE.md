# 환경영향평가 보고서 AI 자동화 프로젝트

## 프로젝트 개요

소규모 환경영향평가 보고서(태양광 발전시설)의 특정 파트를 AI로 자동 생성하는 시스템.
현재 **소음·진동 파트(섹션 7.2.7)** PoC 완료 상태이며, 대기질 등 다른 파트로 확장 예정.

## 현재 상태

- **PoC 결과: GO (진행 가능)**
- 문서 구조 일치도 100%, 변수 치환 정확도 100%, 환각 0건
- 실무 적용 시 작업시간 4~5일 → 1~1.5일 단축 예상

## 프로젝트 구조

```
├── CLAUDE.md                      # 이 파일
├── docs/                          # 프로젝트 문서
│   ├── poc_plan.md                # PoC 실행 계획서
│   ├── poc_result_report.md       # PoC 결과 보고서 (클라이언트용)
│   └── phase0_analysis.md         # 자료 비교 분석 결과
│
├── scripts/                       # 유틸리티 스크립트
│   └── extract_hwp.py             # HWP/HWPX 텍스트 추출 스크립트
│
├── references/                    # 레퍼런스 보고서 (추출된 텍스트)
│   ├── ref_01_여주_광대리/
│   │   ├── 사업개요.txt
│   │   └── 소음진동_본문.txt
│   ├── ref_02_원주_무장리/
│   │   ├── 사업개요.txt
│   │   ├── 소음진동_본문.txt
│   │   └── 환경질측정_보고서.txt
│   ├── ref_03_청양_매곡리/
│   │   ├── 사업개요.txt
│   │   ├── 소음진동_본문.txt
│   │   └── 환경질측정_보고서.txt
│   └── ref_04_천안_화덕리/
│       ├── 사업개요.txt
│       └── 소음진동_본문.txt
│
├── prompts/                       # AI 작업 지침서
│   └── 소음진동_instructions.md   # 소음·진동 파트 생성 지침
│
├── tests/                         # PoC 테스트 케이스
│   └── 소음진동/
│       ├── input/                 # 테스트 인풋 (괴산 금신리)
│       │   ├── 사업개요.txt
│       │   └── 환경질측정_보고서.txt
│       ├── expected/              # 정답지 (실무자 작성본)
│       │   └── 소음진동_본문.txt
│       ├── output/                # AI 생성 결과물
│       │   └── 소음진동_본문.txt
│       └── comparison_result.md   # 비교 분석 결과
│
└── raw_data/                      # 원본 자료 (git에서 제외)
    └── 소규모환경 태양광사업 관련 자료_260219/
```

## 핵심 도메인 지식

### 소규모 환경영향평가 보고서 구조 (소음·진동 파트)

```
가. 현황
   ├── 현황조사내용 표
   ├── (1) 소음·진동 발생원 현황 (서술)
   ├── (2) 소음·진동 현황 (측정지점/일시/결과 표)
   ├── 소음환경기준 표 (법령 고정)
   ├── 생활소음 규제기준 표 (법령 고정)
   ├── 생활진동 규제기준 표 (법령 고정)
   └── 환경분쟁 피해배상액 산정기준 (법령 고정)
나. 영향예측
   ├── 영향예측내용 표
   ├── 영향예측지점 표 (현장조사 필요)
   ├── 예측방법 (공식 + 장비 표)
   └── 예측결과 (소음·진동 각각의 목표/합성/이격거리별/정온시설별 표)
다. 저감방안
   ├── 환경보전목표 표
   └── 공사시 저감방안 (일반 + 소음 + 진동)
```

### 문서 영역 분류

| 영역 | 비율 | 설명 |
|------|------|------|
| 고정 영역 | ~45% | 법령 인용표, 예측 공식, 저감방안 문구 — 모든 보고서에서 동일 |
| 단순 치환 영역 | ~40% | 사업명, 위치, 측정값, 예측값 — 숫자·지명만 교체 |
| 생성 필요 영역 | ~15% | 발생원 현황 서술, 사업별 특이사항 |

### AI가 생성 가능한 범위 (~80%)

- 법령 기준표 3종 + 환경분쟁 기준 (고정)
- 현황 조사내용·측정결과 (인풋에서 추출)
- 건설기계류 표·예측 공식 (고정)
- 합성소음도·진동레벨 계산 (자동)
- 이격거리별 감쇠 표 (자동)
- 저감방안 전체 (고정)

### 실무자 입력이 필요한 범위 (~20%)

- 영향예측지점 (현장조사 데이터)
- 정온시설별 예측값 (이격거리 입력 시 자동 계산 가능)
- 발생원 현황 특이사항 (사업별 서술)

## 기술 스택

### HWP 파일 처리

- **HWP (바이너리)**: `olefile` + `zlib` 라이브러리로 텍스트 추출
  - `tag_id 67` = HWPTAG_PARA_TEXT (UTF-16LE 인코딩)
  - FileHeader offset 36에서 압축 여부 플래그 확인
  - BodyText/Section0 스트림에서 레코드 단위 파싱
- **HWPX (XML 기반)**: `zipfile` + `xml.etree.ElementTree`로 텍스트 추출
  - ZIP 해제 후 section XML 파일에서 텍스트 노드 추출

### AI 모델

- Claude Opus 4.6 / Claude Sonnet 4.6 사용
- 레퍼런스 전체를 단일 컨텍스트에 투입 (약 200K~300K 토큰)
- 일괄 생성(전략 B) 방식 적용

## 작업 지침

### 새로운 파트 확장 시 절차

1. **레퍼런스 수집**: 해당 파트의 실제 보고서 본문 3~5세트
2. **텍스트 추출**: `scripts/extract_hwp.py`로 HWP/HWPX에서 텍스트 추출
3. **구조 분석**: 레퍼런스 간 구조 일치도, 고정/가변 영역 비율 분석
4. **Instructions 작성**: `prompts/` 폴더에 해당 파트 생성 지침 작성
5. **생성 테스트**: 레퍼런스 1개를 정답지로 분리하고 AI 생성 → 비교
6. **결과 보고**: 비교 분석 결과를 `tests/` 폴더에 기록

### 보고서 생성 시 품질 기준

1. **환각 금지**: 인풋에 없는 수치나 지점명을 절대 만들어내지 말 것. 부족하면 `[확인 필요]`로 표시
2. **구조 준수**: 레퍼런스와 동일한 섹션 순서와 표 구조 사용
3. **변수 일관성**: 동일 변수가 여러 곳에 등장할 때 모두 같은 값 사용
4. **문체 통일**: 보고서 문체 준수 ("~으로 조사되었다", "~할 계획이다")
5. **법령 정확성**: 법령 인용표는 레퍼런스에서 그대로 가져올 것

### 코드 작성 시 주의사항

- Python 3.10+ 사용
- 가상환경은 `.venv/` 사용 (`.gitignore`에 포함)
- HWP 처리 시 인코딩은 UTF-16LE, 제어문자(0x0000~0x001F) 필터링 필요
- 원본 HWP/PDF 파일은 `raw_data/`에 보관하고 git에서 제외

## 확장 로드맵

| Phase | 내용 | 상태 |
|-------|------|------|
| Phase 1 | 소음·진동 파트 자동화 | PoC 완료 ✓ |
| Phase 2 | 대기질 파트 확장 | 대기 (자료 수령 필요) |
| Phase 3 | 전체 보고서 통합 (사업개요 + 환경파트 + 삽도) | 미착수 |
| Phase 4 | 웹 기반 운영 시스템 구축 | 미착수 |
