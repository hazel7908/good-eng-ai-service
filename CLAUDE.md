# 환경영향평가 보고서 AI 자동화 프로젝트

## 프로젝트 개요

소규모 환경영향평가 보고서(태양광 발전시설)의 특정 파트를 AI로 자동 생성하는 시스템.
현재 **소음·진동 파트(섹션 7.2.7)** PoC 진행 중.

**접근 방식**: HWPX 템플릿(원주 무장리) + 변수 치환 → 새 사업(괴산 금신리) 보고서 생성

## 현재 상태

- **Phase 1 (소음·진동)**: HWPX 템플릿 방식 PoC 진행 중
- 텍스트 생성 PoC는 완료 (구조 일치도 100%, 변수 치환 정확도 100%, 환각 0건)
- HWPX 파일 생성 스크립트 초안 작성 완료 (`scripts/generate_hwpx.py`)
- **남은 핵심 문제**: 변수 매핑 데이터의 정확성 — 삽도(이미지)에서만 얻을 수 있는 변수 처리

## 프로젝트 구조

```
├── CLAUDE.md                      # 이 파일
├── docs/                          # 프로젝트 문서
│   ├── poc_plan.md                # PoC 실행 계획서
│   ├── poc_result_report.md       # PoC 결과 보고서 (클라이언트용)
│   └── phase0_analysis.md         # 자료 비교 분석 결과
│
├── scripts/                       # 유틸리티 스크립트
│   ├── extract_hwp.py             # HWP/HWPX 텍스트 추출 스크립트
│   └── generate_hwpx.py           # HWPX 템플릿 기반 보고서 생성 스크립트
│
├── references/                    # 레퍼런스 보고서 (추출된 텍스트)
│   ├── ref_01_여주_광대리/
│   ├── ref_02_원주_무장리/
│   ├── ref_03_청양_매곡리/
│   └── ref_04_천안_화덕리/
│
├── prompts/                       # AI 작업 지침서
│   └── 소음진동_instructions.md
│
├── templates/                     # HWPX 템플릿 파일
│   ├── 원주_무장리_소음진동_템플릿.hwpx
│   └── 괴산_금신리_소음진동_정답.hwpx   # 검증용만 (생성 시 참조 금지!)
│
├── tests/                         # 테스트 케이스
│   └── 소음진동/
│       ├── input/                 # 인풋 (사업개요.txt, 환경질측정_보고서.txt)
│       ├── expected/              # 정답지 (실무자 작성본 텍스트)
│       └── output/                # AI 생성 결과물
│
└── raw_data/                      # 원본 자료 (git에서 제외)
```

---

## 핵심 교훈 (이전 세션에서 발견한 실수들)

### 1. 정답지 참조 금지 원칙

> **정답지(괴산 금신리 HWPX)는 생성 과정에서 절대 참조하면 안 된다.**
> 실무에서는 정답지가 없다. 주어진 인풋 자료(사업개요 + 환경질측정 보고서)만으로 생성해야 한다.
> 정답지는 최종 검증 단계에서만 사용한다.

### 2. 변수 데이터 출처 확인 필수

이전 세션에서 삽도(JPG 이미지)에서 추출한 데이터를 사용했는데, 이 데이터가 부정확했다.
변수 값을 사용할 때는 반드시 **원본 텍스트 파일에서 직접 확인**해야 한다.
텍스트에서 추출 불가능한 데이터는 `[확인 필요]` 또는 `[현장조사 필요]`로 표시한다.

### 3. 잘못된 매핑 사례 (반복 금지)

| 항목 | 잘못된 값 (이전 세션) | 올바른 값 (텍스트 인풋 기준) |
|------|---------------------|--------------------------|
| 조사시기 | 09.18~09.19 | 09.22~09.26 (사업개요 추진경위의 "2025.09.: 현장조사") |
| 측정지점 이격거리 | 150m | [확인 필요] (텍스트에서 추출 불가) |
| 측정지점 비고 | 주택 | [확인 필요] (텍스트에서 추출 불가) |
| P-point 데이터 | 삽도에서 추출한 부정확한 값 | [현장조사 필요] (삽도/현장조사 데이터 필요) |
| 진동규제기준 지역 | 가. 주거지역 | 나. 그 밖의 지역 (환경질측정 보고서에서 확인) |

---

## 양식별 변수 매핑 (표 번호 → 변수 → 추출 방법)

### 범례

| 추출 가능 여부 | 의미 |
|:---:|------|
| **O** | 텍스트 인풋에서 자동 추출 가능 |
| **계산** | 다른 변수로부터 공식 자동 계산 |
| **삽도△** | 삽도 이미지에서 추출 가능하나 정밀도 낮음 |
| **X** | 텍스트/삽도로 추출 불가 — 실무자 입력 또는 설계도서 필요 |
| **고정** | 모든 보고서에서 동일 — 템플릿 유지 |

### 가. 현황 섹션

#### 표 7.2.7-1 현황조사내용 (Table 4)

| 셀 | 원주 템플릿 값 | 괴산 정답 값 | 추출 | 추출 방법 |
|----|-------------|-----------|:---:|----------|
| 조사시기 | 2025. 06. 30. ~ 2025. 07. 04. | 2025. 09. 22. ~ 2025. 09. 26. | **X** | 사업개요에 "2025. 09."만 있고 정확한 일자 없음. 실무자 확인 필요 |
| 조사항목 | 소음·진동 발생원 현황... | (동일) | **고정** | |
| 조사범위 | 반경 1.0km, 문헌자료: 1개 지점 | (동일) | **고정** | |
| 조사방법 | 측정자료 | (동일) | **고정** | |

#### 소음·진동 측정지점 표 (Table 5)

| 셀 | 원주 템플릿 값 | 괴산 정답 값 | 추출 | 추출 방법 |
|----|-------------|-----------|:---:|----------|
| 지점주소 | 강원특별자치도 원주시 호저면 생담길 120 | 충청북도 괴산군 청안면 질마로불당재길 48-56 | **O** | 환경질측정_보고서.txt L129 `NV-1` 주소 |
| 이격거리(m) | 250 | 25 | **삽도△** | 소음진동 측정지점.jpg에서 대략 추정 가능. 정확한 값은 실무자 확인 |
| 비고 | 축사 인근 | 이서건설 | **X** | 텍스트/삽도에 없음. 현장조사에서 확인한 주변 시설명 |

#### 소음측정결과 표 (Table 6)

| 셀 | 원주 템플릿 값 | 괴산 정답 값 | 추출 | 추출 방법 |
|----|-------------|-----------|:---:|----------|
| 주간 1~4회 | 46.1, 45.2, 44.0, 44.9 | 46.9, 48.4, 51.3, 48.1 | **O** | 환경질측정_보고서.txt L941-944 |
| 주간 평균 | 45.0 | 49 | **O** | 환경질측정_보고서.txt L945 |
| 야간 1~2회 | 38.1, 39.7 | 43.6, 43.5 | **O** | 환경질측정_보고서.txt L947-948 |
| 야간 평균 | 39.0 | 44 | **O** | 환경질측정_보고서.txt L949 |
| 기준 지역명 | 일반지역 "나" 지역 | 일반지역 "다" 지역 | **O** | 환경질측정_보고서.txt L130 |
| 기준값 주간/야간 | 55 / 45 | 65 / 55 | **O** | 지역구분에서 자동 매핑 ("다" → 65/55) |

#### 진동측정결과 표 (Table 7)

| 셀 | 원주 템플릿 값 | 괴산 정답 값 | 추출 | 추출 방법 |
|----|-------------|-----------|:---:|----------|
| 주간 1~2회 | 9.4, 11.3 | 18.7, 11.6 | **O** | 환경질측정_보고서.txt L966-967 |
| 주간 평균 | 10.0 | 15 | **O** | 환경질측정_보고서.txt L968 |
| 심야 1회 | 9.2 | 9.9 | **O** | 환경질측정_보고서.txt L970 |
| 심야 평균 | 9.0 | 10 | **O** | 환경질측정_보고서.txt L971 |
| 기준 지역명 | 나. 그밖의 지역 | 나. 그밖의 지역 | **O** | 환경질측정_보고서.txt L626 |
| 기준값 주간/심야 | 70 / 65 | 70 / 65 | **O** | 지역구분에서 자동 매핑 |

#### 측정지점도 삽도 (Table 8)

| 항목 | 추출 | 추출 방법 |
|------|:---:|----------|
| 삽도 이미지 | **X** | HWPX 내 BinData 이미지 교체 필요. 소음진동 측정지점.jpg를 사용하되 별도 이미지 처리 작업 |

#### 법령 표 (Table 9~12) — 전부 고정

| 표 | 내용 | 추출 |
|----|------|:---:|
| Table 9 | 소음환경기준 | **고정** |
| Table 10 | 생활소음 규제기준 | **고정** |
| Table 11 | 생활진동 규제기준 | **고정** |
| Table 12 | 환경분쟁 피해배상액 산정기준 | **고정** |

### 나. 영향예측 섹션

#### 표 7.2.7-9 영향예측내용 (Table 13)

| 셀 | 추출 | 추출 방법 |
|----|:---:|----------|
| 사업명 (예측범위 내) | **O** | 사업개요.txt L1 |
| 나머지 | **고정** | |

#### 소음·진동 영향예측지점 표 (Table 14)

| 셀 | 원주(5행) | 괴산(8행) | 추출 | 추출 방법 |
|----|---------|---------|:---:|----------|
| 지점명 | 민가1, 축사1~4 | 민가1, 축사1~5, 민가3, 마을1 | **삽도△** | 정온시설.jpg 라벨 + 영향예측지점.jpg 위치 매칭 |
| 방향 | 남동, 남, 남, 북, 북동 | 북서, 서, 남, 동, 북서, 남서, 남동, 북 | **삽도△** | 영향예측지점.jpg에서 사업부지 기준 방향 판독 |
| 이격거리(m) | 46, 314, 393, 593, 698 | 160, 225, 175, 151, 450, 505, 595, 690 | **삽도△** | 동심원(250m, 500m 등)으로 대략 추정. 정밀도 낮음 |
| XTM, YTM | 좌표 | 좌표 | **X** | GIS/측량 데이터 필요 |
| 비고 | - | 신흥농장, 미영농장, 면사무소 등 | **X** | 현장조사 필요 |

> **행 수 변경**: 원주 5행 → 괴산 8행. `copy.deepcopy()`로 3행 추가 + `rowCnt` 속성 업데이트 필요.

#### 예측지점도 삽도 (Table 15)

| 항목 | 추출 | 추출 방법 |
|------|:---:|----------|
| 삽도 이미지 | **X** | HWPX 내 BinData 이미지 교체. 대기, 소음진동 영향예측지점.jpg 사용 |

#### 건설기계류 소음도 / 공식 (Table 16~18) — 전부 고정

#### 투입장비대수 표 (Table 19)

| 셀 | 원주 값 | 괴산 정답 값 | 추출 | 추출 방법 |
|----|-------|-----------|:---:|----------|
| 장비명/규격 | 굴삭기 1.0m³ + 덤프 15ton | (동일) | **고정** | |
| 일작업량 (m³/일) | 201.22 | 190 | **X** | 토공량 ÷ 작업일수. 사업개요에 토공량 없음. 설계도서 필요 |
| 시간당작업량 (m³/hr) | 25.15 | 23.75 | **X** | 일작업량 ÷ 8hr. 일작업량에 의존 |
| 장비별작업량 | 138.9 / 57.2 | (동일) | **고정** | |
| 투입대수 | 각 1대 | (동일) | **고정** | |

#### 공종별 합성소음도 표 (Table 20)

| 셀 | 추출 | 추출 방법 |
|----|:---:|----------|
| 장비 구성 | **고정** | 굴삭기+덤프 → 합성소음도 76.6 (4개 레퍼런스 중 3개가 이 구성) |
| 합성소음도 | **계산** | `10*log10(10^(71.7/10) + 10^(74.9/10))` = 76.6 |

#### 이격거리별 소음도 표 (Table 21)

| 셀 | 원주 값 | 괴산 정답 값 | 추출 | 추출 방법 |
|----|-------|-----------|:---:|----------|
| 거리 헤더 | 57, 101, 150, 200, 300, 500, 1000 | 57, 100, 150, 200, 300, 500, 1000 | **계산** | 101→100 차이. 합성소음도 기준 65dB 도달거리에서 시작 |
| 소음도 값 | 65.0, 60.0, 56.6, ... | 65.0, 60.1, 56.6, ... | **계산** | `76.6 - 20*log10(d/15)` |

#### 정온시설 예측소음도 표 (Table 22)

| 셀 | 추출 | 추출 방법 |
|----|:---:|----------|
| 지점명 | **삽도△** | Table 14와 동일 |
| 이격거리 | **삽도△** | Table 14와 동일 |
| 예측소음도 | **계산** | `76.6 - 20*log10(거리/15)` |
| 소음기준 | **계산** | 지점 종류에 따라 자동: 민가/마을=65, 축사=60 |
| 만족여부 | **계산** | 예측소음도 ≤ 소음기준 → 만족 |

> **행 수 변경**: 원주 5행 → 괴산 8행

#### 공종별 합성진동레벨 표 (Table 23)

| 셀 | 추출 | 추출 방법 |
|----|:---:|----------|
| 합성진동레벨 | **계산** | `10*log10(10^(33.5/10) + 10^(33.3/10))` = 36.4 |

#### 이격거리별 진동도 표 (Table 24)

| 셀 | 추출 | 추출 방법 |
|----|:---:|----------|
| 거리 헤더 | **계산** | 50, 100, 150, 200, 300, 500, 1000 |
| 진동레벨 값 | **계산** | `36.4 - 16.17*log10(d/7.5)` |

#### 정온시설 예측진동도 표 (Table 25)

| 셀 | 추출 | 추출 방법 |
|----|:---:|----------|
| 지점명/이격거리 | **삽도△** | Table 14와 동일 |
| 예측진동도 | **계산** | `36.4 - 16.17*log10(거리/7.5)` |
| 진동기준 | **계산** | 민가/마을=70, 축사=57 |
| 만족여부 | **계산** | 예측진동도 ≤ 진동기준 → 만족 |

> **행 수 변경**: 원주 5행 → 괴산 8행

### 다. 저감방안 섹션

#### 소음·진동 환경보전목표 표 (Table 26)

| 셀 | 추출 | 추출 방법 |
|----|:---:|----------|
| 소음 기준 | **고정** | 주거시설 65, 축사 60 |
| 진동 기준 | **고정** | 주거시설 65, 축사 57 |

#### 저소음 건설장비 / 분산투입 (Table 27~28) — 전부 고정

#### 최종 저감대책 수립 후 예측소음도 표 (Table 29)

| 셀 | 추출 | 추출 방법 |
|----|:---:|----------|
| 지점명/이격거리 | **삽도△** | Table 14와 동일 |
| 예측치 | **계산** | Table 22 예측소음도와 동일 |
| 저소음장비 후 | **계산** | 예측치 - 저소음장비 저감량 (굴삭기 0.9dB) |
| 저소음+분산투입 후 | **계산** | 저소음장비 후 - 분산투입 저감량 |
| 최종예측치 | **계산** | = 저소음+분산투입 후 |
| 환경보전목표 | **계산** | 민가/마을=65.0, 축사=60.0 |

> **행 수 변경**: 원주 5행 → 괴산 8행
> **참고**: 괴산은 모든 P-point가 기준 만족이지만, 정답지에는 최종 저감대책 표가 포함됨. 분기 로직 수정 필요 — 원주 템플릿에 이 표가 있으므로 항상 포함하는 것이 맞을 수 있음.

### 본문 서술 (테이블 외부 텍스트)

| 서술 위치 | 원주 값 | 괴산 값 | 추출 | 추출 방법 |
|----------|-------|-------|:---:|----------|
| 사업명 (여러 곳) | 원주시 호저면 무장리... | 괴산군 청안면 금신리... | **O** | 사업개요.txt L1 |
| 소음 측정결과 서술 | 주간 평균 45.0dB(A), 야간 평균 39.0dB(A) | 주간 평균 49.0dB(A), 야간 평균 44.0dB(A) | **O** | 환경질측정_보고서.txt L945, L949 |
| 진동 측정결과 서술 | 주간 평균 10.0dB(V), 야간 9.0dB(V) | 주간 평균 15.0dB(V), 야간 10.0dB(V) | **O** | 환경질측정_보고서.txt L968, L971 |
| 측정일시 서술 | 2025년 06월 24일 ~ 06월 25일 | 2025년 09월 18일 ~ 09월 19일 | **O** | 환경질측정_보고서.txt L57 |
| 소음기준 지역 서술 | '나' 지역 | '다' 지역 | **O** | 환경질측정_보고서.txt L130 |
| 진동기준 지역 서술 | '가' 지역 65dB(V) | '나' 지역 70dB(V) | **O** | 환경질측정_보고서.txt L626 |
| P-point 수 서술 | 5개 지점 | 8개 지점 | **삽도△** | 영향예측지점.jpg에서 P-point 개수 카운트 |
| 예측결과 서술 | P-1 지점을 제외한 전 지점에서... | 전 지점에서 기준치를 만족... | **계산** | 소음 초과 여부에 따라 문구 자동 선택 |
| 발생원 현황 서술 | 사업계획지구 주변으로 농경지... | (사업별 서술) | **X** | 현장조사 + AI 생성 필요 |

### 머리글/바닥글 (Table 0~1) + header.xml

| 항목 | 추출 | 추출 방법 |
|------|:---:|----------|
| 사업명 | **O** | 사업개요.txt L1. section0.xml Table 0~2 + header.xml 모두 치환 |

---

### 추출 가능 여부 요약

| 분류 | 테이블/항목 수 | 설명 |
|------|:----------:|------|
| **O (자동 추출)** | 6개 표 + 본문 8개소 | 텍스트 파일에서 정확한 값 추출 |
| **계산 (자동)** | 7개 표 | 공식으로 자동 계산 (합성소음도, 이격거리별 등) |
| **고정 (유지)** | 8개 표 | 법령표, 공식, 저감방안 등 |
| **삽도△ (부분 추출)** | 1개 표 (Table 14) → 3개 표에 전파 | 지점명·방향은 삽도에서 추출 가능, 이격거리는 정밀도 낮음, 좌표/비고 불가 |
| **X (추출 불가)** | 3개 항목 | 조사시기 일자, 투입장비 작업량, 측정지점 비고 |
| **이미지 교체** | 2개 삽도 | Table 8, 15 — BinData 이미지 교체 필요 |

---

## 보고서 구조 분기 로직

4개 레퍼런스 비교에서 발견한 핵심 분기:

### P-point 소음기준 초과 여부에 따른 구조 변화

```
IF 모든 P-point가 소음기준 만족:
    → 단순 구조 (ref_01 여주, ref_04 천안)
    → 저감방안: "필요시 저소음 건설장비 사용"
    → 최종 저감대책 표: 없음

IF 일부 P-point가 소음기준 초과:
    → 확장 구조 (ref_02 원주, ref_03 청양)
    → 저감방안: 저소음장비 → 분산투입 → (필요시) 가설방음판넬
    → 최종 저감대책 표: 있음 (예측치/저소음장비 후/분산투입 후/최종예측치/환경보전목표)
```

### 투입장비 수에 따른 계산 차이

```
굴삭기만 (ref_01):
    합성소음도 = 71.7 dB(A), 합성진동 = 33.5 dB(V)

굴삭기 + 덤프트럭 (ref_02, 03, 04):
    합성소음도 = 76.6 dB(A), 합성진동 = 36.4 dB(V)
```

### P-point 수: 프로젝트마다 다름

ref_01: 6개, ref_02: 5개, ref_03: 10개, ref_04: 7개
→ 테이블 행 수를 동적으로 조절해야 함 (deepcopy로 행 복제/삭제)

---

## HWPX 기술적 주의사항

### HWPX 파일 구조

HWPX = ZIP 파일. 내부 구조:
```
├── mimetype                    # "application/hwp+zip" (반드시 ZIP_STORED!)
├── META-INF/
│   └── container.xml
├── Contents/
│   ├── section0.xml            # 본문 (모든 텍스트와 테이블)
│   ├── header.xml              # 머리글
│   └── ...
├── BinData/                    # 이미지 등 바이너리
└── PrvImage.png                # 미리보기 이미지 (ZIP_STORED!)
```

### XML 구조 (section0.xml)

```xml
<hp:tbl>          <!-- 테이블 -->
  <hp:tr>           <!-- 행 -->
    <hp:tc>           <!-- 셀 -->
      <hp:subList>
        <hp:p>          <!-- 단락 -->
          <hp:run>        <!-- 런 -->
            <hp:t>텍스트</hp:t>  <!-- 실제 텍스트 -->
          </hp:run>
        </hp:p>
      </hp:subList>
    </hp:tc>
  </hp:tr>
</hp:tbl>
```

### 필수 주의사항 (버그 방지)

1. **ZIP 압축 타입 보존**: `mimetype`과 이미지 파일은 `ZIP_STORED` (compress_type=0)이어야 함.
   `ZIP_DEFLATED`로 쓰면 한글 프로그램에서 파일이 열리지 않음.
   ```python
   # 원본의 압축 타입을 읽어서 보존
   compress_types = {}
   with zipfile.ZipFile(template, 'r') as z:
       for info in z.infolist():
           compress_types[info.filename] = info.compress_type
   ```

2. **XML 네임스페이스 등록**: `ET.register_namespace()`로 모든 네임스페이스를 등록해야 함.
   안 하면 출력 시 `ns0:`, `ns1:` 같은 프리픽스로 바뀌어 파일이 깨짐.
   ```python
   NAMESPACES = {
       "hp": "http://www.hancom.co.kr/hwpml/2011/paragraph",
       "hs": "http://www.hancom.co.kr/hwpml/2011/section",
       # ... (generate_hwpx.py 참조)
   }
   for prefix, uri in NAMESPACES.items():
       ET.register_namespace(prefix, uri)
   ```

3. **hp:t 텍스트 분리 문제**: 하나의 문장이 여러 `<hp:run><hp:t>` 요소에 걸쳐 있을 수 있음.
   예: `"주간 평균 "` + `"10.0"` + `"dB(V), 야간 평균 "` + `"9.0"` + `"dB(V)으로"`
   → 단순 문자열 치환이 안 될 수 있으므로, 개별 `hp:t` 노드를 순회하며 정확한 값 매칭 필요.

4. **테이블 행 동적 추가**: P-point 수가 다르면 테이블 행을 추가/삭제해야 함.
   ```python
   import copy
   last_row = rows[-1]
   new_row = copy.deepcopy(last_row)
   # hp:t 텍스트 설정 후
   table.insert(list(table).index(last_row) + 1, new_row)
   # hp:tbl의 rowCnt 속성도 업데이트
   table.set('rowCnt', str(new_row_count))
   ```

5. **테이블 인덱스 매핑** (원주 무장리 템플릿 기준):

   | Index | 내용 | 비고 |
   |-------|------|------|
   | 0~1 | 머리글/바닥글 | 사업명 치환 |
   | 2 | 사업명 | 치환 |
   | 3 | 장 제목 | 유지 |
   | 4 | 현황조사내용 | 조사시기 치환 |
   | 5 | 소음·진동 측정지점 | 주소/이격거리/비고 치환 |
   | 6 | 소음측정결과 | 측정값 + 기준지역 치환 |
   | 7 | 진동측정결과 | 측정값 치환 |
   | 8 | 측정지점도 (삽도) | 이미지 교체 필요 |
   | 9~12 | 법령표 (고정) | 유지 |
   | 13 | 영향예측내용 | 사업명만 치환 |
   | 14 | 영향예측지점 | P-point 데이터 (C1) |
   | 15 | 예측지점도 (삽도) | 이미지 교체 필요 |
   | 16~18 | 소음도표/공식 (고정) | 유지 |
   | 19 | 투입장비대수 | 작업량 치환 (C4) |
   | 20 | 합성소음도 | 자동계산 (B1) |
   | 21 | 이격거리별 소음도 | 자동계산 (B3) |
   | 22 | 정온시설 예측소음도 | P-point별 (B5) |
   | 23 | 합성진동레벨 | 자동계산 (B2) |
   | 24 | 이격거리별 진동도 | 자동계산 (B4) |
   | 25 | 정온시설 예측진동도 | P-point별 (B6) |
   | 26 | 환경보전목표 | 유지 (대부분 동일) |
   | 27 | 저소음 건설장비 (고정) | 유지 |
   | 28 | 분산투입 효과 (고정) | 유지 |
   | 29 | 최종 저감대책 후 예측소음도 | 분기 로직에 따라 (B7, B8) |

---

## 레퍼런스 비교 요약 (4개 사업)

### 핵심 차이점

| 항목 | ref_01 여주 | ref_02 원주 | ref_03 청양 | ref_04 천안 |
|------|-----------|-----------|-----------|-----------|
| P-point 수 | 6 | 5 | 10 | 7 |
| 장비 구성 | 굴삭기만 | 굴삭기+덤프 | 굴삭기+덤프 | 굴삭기+덤프 |
| 합성소음도 | 71.7 | 76.6 | 76.6 | 76.6 |
| 소음기준 지역 | "다" (65/55) | "나" (55/45) | "다" (65/55) | "다" (65/55) |
| 진동기준 지역 | 가. 주거지역 | 나. 그밖의 지역 | 나. 그밖의 지역 | 가. 주거지역 |
| 소음 초과 P-point | 없음 | P-1 (66.9>65) | P-1 (78.5>60) | 없음 |
| 저감대책 구조 | 단순 | 확장 | 확장+방음판넬 | 단순 |
| 조사방법 | 문헌자료 | 측정자료 | 현지조사 | 문헌자료 |

---

## 작업 지침

### HWPX 보고서 생성 절차

1. **인풋 파일 읽기**: 사업개요.txt + 환경질측정_보고서.txt
2. **A 변수 추출**: 텍스트에서 자동 추출 가능한 변수들
3. **C 변수 확인**: 텍스트에서 추출 불가능한 변수 → `[확인 필요]` 표시
4. **B 변수 계산**: A + C 변수를 이용한 자동 계산
5. **템플릿 치환**: XML 텍스트 치환 + 테이블 셀 치환 + 행 추가/삭제
6. **ZIP 재포장**: 압축 타입 보존하여 HWPX 저장
7. **검증**: 한글에서 열어 서식/레이아웃 확인

### 보고서 생성 시 품질 기준

1. **환각 금지**: 인풋에 없는 수치나 지점명을 절대 만들어내지 말 것. 부족하면 `[확인 필요]`로 표시
2. **구조 준수**: 레퍼런스와 동일한 섹션 순서와 표 구조 사용
3. **변수 일관성**: 동일 변수가 여러 곳에 등장할 때 모두 같은 값 사용
4. **문체 통일**: 보고서 문체 준수 ("~으로 조사되었다", "~할 계획이다")
5. **법령 정확성**: 법령 인용표는 레퍼런스에서 그대로 가져올 것

### 코드 작성 시 주의사항

- Python 3.10+ 사용
- 가상환경은 `.venv/` 사용 (`.gitignore`에 포함)
- 원본 HWP/PDF 파일은 `raw_data/`에 보관하고 git에서 제외

---

## 현재 TODO

### generate_hwpx.py 수정 필요 사항

1. **ZIP 압축 타입 보존 추가** — 현재 스크립트는 모든 파일을 ZIP_DEFLATED로 쓰므로 한글에서 안 열림
2. **조사시기(Table 4) 값 재확인** — 사업개요에서 정확한 현황조사 기간 확인 필요
3. **진동규제기준 지역 확인** — "다" 지역은 소음환경기준, 진동은 "나. 그 밖의 지역"
4. **테이블 행 동적 추가 기능** — P-point 수가 템플릿(5개)과 다를 때 행 추가/삭제
5. **소음 초과 분기 로직 구현** — 초과 시 확장 구조, 미초과 시 단순 구조

### 향후 작업

| Phase | 내용 | 상태 |
|-------|------|------|
| Phase 1-1 | 소음·진동 텍스트 생성 PoC | 완료 |
| Phase 1-2 | HWPX 템플릿 기반 파일 생성 | 진행 중 |
| Phase 2 | 대기질 파트 확장 | 대기 (자료 수령 필요) |
| Phase 3 | 전체 보고서 통합 | 미착수 |
| Phase 4 | 웹 기반 운영 시스템 구축 | 미착수 |
