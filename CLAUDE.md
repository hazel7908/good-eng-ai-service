# 환경영향평가 보고서 AI 자동화 프로젝트

소규모 환경영향평가 보고서(태양광 발전시설)의 파트별 초안을 AI로 자동 생성한다.
**베이스 문서(빈칸 뚫린 HWPX) + 변수 명세 + 신규 사업 인풋 → 생성.**
기술: Windows + 한글 프로그램 API (win32com) + Python 3.10+

> 이 파일은 **항상 로드**된다. 짧게 유지할 것 — 분량이 늘면 지시 준수율이 떨어진다.
> 구조·PoC 결과·문서 목록은 [`README.md`](README.md), 설계 근거는 [`docs/knowledge_architecture.md`](docs/knowledge_architecture.md).

---

## 폴더 3분할 — 이것만은 헷갈리면 안 됨

| 폴더 | 정체 | 생성 중 접근 |
|---|---|:--:|
| `templates/` | 베이스 문서 (빈칸 뚫린 시작 파일) | ✅ |
| `golden/` | **골든셋 = 완성된 정답 보고서** | ❌ **금지** |
| `cases/{카테고리}/{사업}/` | 사업별 입력(`input/`) · 변수(`vars/`) · 생성물(`{파트}/`) | ✅ |

`engine/` 생성 엔진 · `catalog/` NAS 카탈로그 · `_archive/poc-2026-03/` 옛 PoC(**참조 금지**)

---

## 핵심 원칙

### 1. 정답지 참조 금지

`golden/` 은 **검증 단계에서만** 연다. 실무에는 정답이 없다. 생성은 인풋(사업개요 + 환경질측정 보고서)만으로 한다.

### 2. 환각 금지 — 없으면 `[확인 필요]`

인풋에 없는 수치·지점명·파트 번호를 **만들어내지 않는다.** 부족하면 `[확인 필요]` / `[현장조사 필요]` 로 표시한다.

### 3. 원본 텍스트 우선

변수 값은 **텍스트 파일에서 직접 확인**한다. 삽도(JPG) 추출값은 부정확하다 — 2차 PoC 오류 61건 중 **10건이 삽도 추출 부정확**이었다.

### 4. 기준 체계 혼동 주의 ⚠️

소음·진동 기준이 **3가지 서로 다른 체계**로 나뉜다(소음환경기준 "가"~"라" / 생활소음규제 가·나 / 생활진동규제 가·나). 혼동이 2차 PoC의 Critical 오류 1번이었다. → `.claude/rules/common.md`

### 5. 규칙에는 근거 건수를 적는다 `(n/7)`

> ⚠️ **정답지도 자기 안에서 어긋난다.** 같은 값이 표마다 다른 자리가 실제로 13곳 나왔다.
> `(n/7)` 을 셀 때 **한 문서 안의 여러 표를 교차 검증**할 것 — 한 표만 보면 잘못 센다.
> → `golden/small-env/_variants.md` §7


파트 규칙은 골든셋에서 도출한다. **한 사업에서만 관측된 것을 법칙으로 쓰지 않는다.** `(1/7)` 항목은 그 사업의 우연일 수 있다. **골든셋 7건** (`golden/small-env/_variants.md` §8).

> 실제로 2026-07-29 재검증에서 **괴산 1건에서 일반화된 규칙 3개가 틀린 것**으로 드러났다 (주거 진동 기준·목표기준 지역 문자·저감량 계산). → `golden/small-env/_variants.md`

---

### 6. 표를 채울 때 — 커서를 어림으로 움직이지 않는다 ⚠️

**앵커에서 절대 오프셋(행·열)으로 간다. 이어서 움직이지 않는다.**
공용 규약 `hwp_util.write_at()`. 2026-08-31~09-01 여섯 파트에서 **함정 10가지**가 나왔고
전부 이 한 줄로 수렴한다 — 세로 병합은 **행·열 양쪽**에서 커서를 왜곡한다.
⚠️ 구조는 **셀 주소로만** 읽는다 (평면 추출로 세다 세 번 틀렸다). `KeyIndicator()` 도 되지만
**HWPX XML 의 `cellAddr`·`cellSpan` 직독이 빠르고 병합 폭이 숫자로 나온다** — 재해 1장은
앵커 추정 6자리가 전부 틀렸고 XML 한 번으로 확정했다. **병합 폭을 모르면 `col_off` 를 셀 수 없다.**
⚠️ **자료가 없으면 건너뛰지 말고 비운다** — 건너뛰면 기준 사업 값이 그대로 실린다.
→ 상세·사례: `.claude/rules/hwpx.md` §표 채우기 · §한글 API 운영

### 7. 검증 도구는 셋 다 조용히 통과시킨다 🚨

- **`smoke_check` 는 표 유출을 못 잡는다** (검사 신설은 Mac — 증거·오탐 목록은 [`20260901_표유출검사_증거인계.md`](docs/20260901_표유출검사_증거인계.md)) — `leak_check` 가 **서술 문장만** 본다.
  0100·0500·0726 이 "통과"인데 원주 값이 실려 있었다. 표 유출은 **값 대조**로만 잡힌다.
- **되먹임은 기준 사업 고유값에 의존하는 코드를 못 잡는다** — 0726 이 `원주시` 를 앵커로
  썼는데 되먹임은 시군이 원주시라 **경고 0건으로 통과**했다. 되먹임=배치 · 다른 사업=고유값,
  **둘 다 돌려야 한다.**
- **dry-run 은 "남은 게 있나"를 안 본다** — spec 누락 3건이 전부 **베이스 텍스트를 뽑아
  기준 사업 지명을 세서** 나왔다. → **베이스 빌드 직후 지명 세기를 필수 절차로.**

- **검사기 자신도 조용히 빗나간다** — `table_leak` 의 되먹임 스킵이 `vars.사업.시군` 만 보는데
  재해 vars 엔 그 키가 없어 **자기 지명 14건이 거짓 실패**로 떴다. 반대로 `지정면적` ⊂ `지정면`
  같은 **부분문자열 덫**은 멀쩡한 산출물을 막는다. 게이트가 빨개지면 **먼저 검사기를 의심할 것.**

⚠️ **가장 위험한 것은 뒤섞인 값이다.** 일괄 치환이 주소 안까지 건드려
`강원도 원주시 가현동 156` → **`강원도 천안시 가현동 156`**. 남의 값이 남는 것보다 나쁘다 —
존재하지 않는 값이 그럴듯해 검토자가 못 걸러낸다.

---

## 지식은 경로로 로드된다

파일을 **여는 행위 자체**가 트리거다. Claude의 판단·기억이 개입하지 않는다.

| rule | 로드 시점 |
|---|---|
| `rules/common.md` | 항상 (`paths:` 없음) |
| `rules/small-env/_category.md` | `cases/`·`templates/`·`golden/` 의 `small-env/` |
| `rules/small-env/noise-vib.md` | 그중 소음진동 파트 |
| `rules/{small-disaster,disaster-impact,disaster-review}/` | 〃 각 재해 카테고리 (09-01 — 재평은 소재평 rule 준용) |
| `rules/hwpx.md` | `engine/` · `templates/` |

⚠️ **`paths:` 가 틀리면 에러 없이 조용히 로드가 안 된다.** 폴더·파일 이름을 바꾸면 반드시 재검증 → [`docs/repo_restructure_plan.md`](docs/repo_restructure_plan.md) §1, 이름 규칙은 [`docs/naming.md`](docs/naming.md)

---

## 현재 상태

> 갱신일 2026-09-01. ★ **소환 11파트 + 소재평 3장 베이스 확보** — 소환 0100·0200·0300·
> 0400·0500·0721·0722·0723·0724·0726·0727 · 소재평 1·2·7장(기준=천안 삼성리 서식).
> 전부 기준 사업 되먹임으로 표 배치까지 검증했다. 커버리지 `engine/coverage.py`
> **유형 균등 24.8% · 실무 건수 가중 43.4%**.
> 소음진동·대기질 코드 오류 0 · **지역개황 천안 63.1%**(84항목, **WRONG 0**) · NAS 전수 조사 완료.
> 상세 이력은 `git log` + `docs/repo_restructure_plan.md` §9.
>
> ⚠️ **2026-08-31 방침 전환** — 도장깨기(파트별 완성) → **수평 러프 + 사람 보완 루프**.
> 10월 말까지 환경 3유형·재해 3유형 전부 최소 C(틀), 주력 B, 기존 3파트 A 유지.
> 파트당 타임박스 2~4일 · WRONG 0 원칙 유지 · 정밀 채점기는 보완 단계로.
> → [`docs/20260831_전체범위_작업전환계획.md`](docs/20260831_전체범위_작업전환계획.md)

| 단계 | 내용 | 상태 |
|:--:|---|:--:|
| 0~3.5 | 구조 개편 + 경로 스코핑 검증 | ✅ |
| 4 · 4.5 | 파트 지식 7장르 재편집 · 카테고리층 · 스킬 정리 | ✅ |
| 5 | 사업 데이터·계산 분리 (`vars/` + `calc.py`) | ✅ |
| 7 · 8 | `distill-golden` 스킬 · 문서 동기화 | ✅ |
| **6** | **원주 골든셋 → 베이스 문서(빈칸 뚫기)** | ✅ 2026-07-31 |
| **9** | **지역개황 통계 소싱** — 자료 19종 중 15종 · 절 18/19 · vars 빌더 | ✅ 2026-08-24 |
| **10** | **지역개황 베이스 문서 + §B 표 22개 + 생성·측정** | ✅ 2026-08-25 (천안 84항목 중 OK+문형 53 = **63.1%** · **WRONG 0**) |

베이스 문서 `templates/small-env/noise-vib.hwpx` (토큰 25종). 수작업 대신 **`engine/build_template.py` 로 재현 가능**하다 — 가운뎃점 3종·스마트따옴표를 손으로 치면 매칭이 깨진다.

`engine/generate.py` 는 win32com 으로 **실행 검증됨** (괴산 생성 성공, 빈칸 잔여 0).

### 측정 결과 — 사업 3건

| 사업 | OK | 리포트 |
|---|:--:|---|
| 괴산 금신리 | 36/61 = **59.0%** (2차 PoC 44.3%) | `cases/small-env/괴산_금신리/noise-vib/validation.md` |
| **천안 화덕리** | 73/77 = **94.8%** (최초 44.2%) — **PP 텍스트화로 WRONG 0** | `cases/small-env/천안_화덕리/noise-vib/validation.md` |
| **청양 매곡리** | 43/86 = **50.0%** (최초 36.0%) | `cases/small-env/청양_매곡리/noise-vib/validation.md` |
| **평창 수청리 (대기질)** | 85/138 = **61.6%** (08-06 결함 수정 후). **코드로 고칠 WRONG 0건** — 남은 것은 인풋 부재 14·베이스 구조 14·표준 대기 7·정답지 불일치 5 | `cases/small-env/평창_수청리/air-quality/validation.md` §8 |

청양은 **구조적으로 가장 이질적인 사업**이다. 계산 규칙은 거의 다 맞았고 **문서 구조가 안 맞았다** —
원주 베이스에 없는 절이 3개(정온·개발시설 분포 / 지형 감쇠 / **전파경로 차단대책 45줄**) 통째로 들어간다.
**빈칸 치환으로 도달할 수 없는 영역**이 처음 드러났다. 항목 수가 달라 비율 비교는 무의미하다.

PP 이격거리는 **텍스트 출처라 9/10 일치** (삽도 판독 평균오차 24m 와 대비). 틀린 1건은
정답지가 `771`(표 14·22·25) ↔ `71`(표 29) 로 **자기 안에서 갈린다.**

베이스 문서 토큰 **25종 → 35종**, 저감 확장절 분기 구현. 천안 문서 줄 수 833 → 730 (정답 736).
~~천안 남은 오류 16건은 삽도 판독 오차~~ → **대기질편 PP 텍스트로 전부 해소** (08-06). 인풋 경로가 해법이었다.

> ⚠️ **괴산으로는 규칙 개선을 측정할 수 없다.** 괴산은 골든셋 7건 중 예외라
> **규칙을 옳게 고칠수록 점수가 떨어진다.** 천안으로 재측정한 이유다.

### 규칙 검증 — 골든셋 7건 ★

**확정**: R1 주거 진동 65 · R2 목표기준 ‘가’ **6:1** (괴산만 예외) · R4 감쇠 16.2 7/7 ·
축사 60/57 7/7 · **①②③ 사업은 PP 라벨 `P-1` + 분산후 `(-4.9)` 병기** 2/2

**여전히 갈림 → 전부 `[실무자 확인]`**: 진동 서술 4:3 · 소음 서술 4:2 · 환경보전목표 행 4:3 ·
측정일시 월 반복 4:3 · **상회 0건일 때 최종표 2:2** · 표 21·24 구분 거리 **제각각** ·
§3-3 반올림 경로 **괴산↔청양 상반**

> ⚠️ **n 이 3 미만이면 기본값으로만 쓰고 규칙으로 굳히지 말 것.** 두 번 데었다 —
> `(2/3)` 으로 세운 표 21 규칙이 4번째에서 `2:2` 가 됐고, 옥천 직후 세운 **"두 계열" 가설은
> 같은 날 청주·평창으로 철회**했다. → `_variants.md` §6-3·§8

### 다음 할 일

| # | 내용 | 근거 |
|:-:|---|---|
| **★** | **전면 전환 (08-31)** — W1: 소환 **수질(0723)+기상(0721)** B 수준 착수 → 소환 전 파트 → 재해 3종 서식 수확 → 본환·전략 이식. 삽도는 동결하되 **삽도별 레이어드 PSD 산출** ✅ **완료 08-31** (`engine/figure_psd.py` · `build_figures_regional.py --psd` · 실전 6건 통과). 아래 기존 할 일들은 이 계획의 주간 슬롯·보완 단계로 흡수. **진행**: R1·R2·R3·R5 리팩터 ✅(engine/parts/ 분리, dry-run 바이트 동일 — **tables 층 Windows 회귀도 ✅ 08-31**: 소음진동·지역개황 텍스트 추출 대조 **0줄**, 리팩터 전 엔진과도 동일) · 수질 골든 7건 확보·추출 ✅ · `calc_water.py` 괴산 역산 10건 ✅ · 우수유출 **두 계열**(2024=6차 다항식·50년 ↔ 2025 후반=조견표·30년, `_variants.md` §11) — 수질 **Mac 체인 완결** ✅ (rule·slots/spec·핸들러·calc_water 42건·천안 vars dry-run) — ★원값 연쇄 대통일(§3-0) · **Windows 세션 ✅ 완료 08-31** ([`20260831_윈도우_수질베이스.md`](docs/20260831_윈도우_수질베이스.md)) — ①리팩터 회귀 0줄 ②수질 베이스 **토큰 30/30** ③천안 첫 생성 **원주 값 0 · `[확인 필요]` 216 · PDF 11쪽 육안**. 베이스는 NAS 원본을 받아 hwp→hwpx 변환까지 **전부 코드로** 만든다 — 지시서가 손으로 하라던 계산 필드 3곳도 빌더에 넣었다(`replace_para` + spec `paras`; **한글 계산 필드는 찾기/바꾸기가 안 걸린다**). ⚠️ **`smoke_check` 가 표 유출을 통과시킨다** — `leak_check` 는 서술 문장만 본다. 첫 생성물에 원주 하천일람(섬강·한강)·IDF·배수유역 값이 실렸는데 `유출 ✅` 였다. 표 유출은 값 대조로 직접 볼 것. ⚠️ **자료가 없을 때 표를 건너뛰면 원주 값이 남는다** — 비운다(행 수는 유지, 라벨만 남김; `fit_rows(need=0)` 로 다 지우면 뒤 표의 앵커까지 사라진다). ⚠️ 앵커·오프셋은 **셀 주소(`KeyIndicator`)로 실측**해야 한다 — `배수유역 1` 은 첫 데이터 행(0부터), `순간 혼합농도`는 머리행이 두 줄(3·4행), 하천일람은 **9열**(slots.md 의 8열은 오기였다). **n>0 채움 경로 ✅ 검증** (원주 2유역 자기 되먹임, 표 7/7 셀 단위 일치 · 줄이기 n=1 잔존 0; n≥3 늘리기는 3유역 사업이 없어 미검증). **기상(0721) ✅ 완료** — 베이스 토큰 39/39 · 천안 첫 생성 원주 값 0 · `[확인 필요]` 278 · PDF 8쪽 육안. 🚨 **표 채우기 결함은 한 뿌리다 — 커서를 이어서 움직이지 말 것.** 두 파트에서 5가지가 같은 원인으로 나왔다: ①앵커가 머리행이 아님(오프셋 밀림) ②머리행이 두 줄(부머리행에 씀) ③`right()` 가 행을 넘긴 뒤 `down()`(한 행씩 건너뜀) ④`fit_rows`·`blank_row` 가 skip 미전달(다른 표로 갈아탐) ⑤**앵커 셀을 덮어씀**(둘째 행부터 전부 실패 — 역순으로 채운다). → 규칙: **행마다 앵커에서 다시 찾아 절대 오프셋으로 간다.** ⚠️ **자료가 없으면 건너뛰지 말고 비운다** — `if vals and find_in_table(...)` 는 기준 사업 값을 그대로 남긴다(파트 무관). ⚠️ **평면화된 추출 텍스트로 표 구조를 세면 안 된다** — 한 셀 안 런이 여러 줄로 보인다. 셀 주소(`KeyIndicator`)로 읽을 것. ★ **파트마다 기준 사업 되먹임 검증을 돌릴 것** — 천안은 vars 가 러프(B)라 **표 채움 경로가 한 번도 안 돈다.** 기준 사업(원주)을 그 자신의 베이스에 되먹이면 행·열 배치가 원본과 대조된다(값은 자기 자신이라 채점이 아니다). 수질 5건·기상 3건이 이 방법으로만 잡혔다. 특히 ⑥ **빈칸 치환이 앵커와 같은 문자열을 새로 만드는** 결함은 채움 경로에서만 나타난다 — 기상 C2 앵커 `2014` 가 치환 뒤 조사내용 표에 생겨 **그 표를 파괴**했는데 천안으로는 영원히 안 잡힌다. 앵커는 **덮어쓰지 않는 머리행 라벨**로 잡을 것. **08-31 심야(Mac)**: 파트 채점 8건 재개 — 실결함 2건뿐: ①천안 0726 성상별 5표 **원주 통계 유출**("값 없음, 스킵"이 함정 그대로 — 핸들러 선비우기 패치, Windows 검증 대기) ②원주 수질 vars 측정값 7건이 **지어낸 값**(러프 데모 환각 — 인풋 실측으로 정정. ⚠️ vars 러프값도 환각 금지, 인풋에서 베낄 것). 나머지 WRONG 은 채점기 `gold_table` 경계 아티팩트(셀 값 `2.0` 을 절 `2.N` 로 오인·파트식 소제목 미인식 — Mac 후속). **0500 환경현황 Mac 체인 ✅**(spec 29토큰 문자열 28/29 검증·vars 빌더 `build_env_status_vars.py`(측정 3파트 복사, §승계)·핸들러 표 12개 — 소환 11파트째, Windows 베이스만 남음 → ✅ 베이스·원주 생성 09-01) · **5.3 통계연보 소싱 n=2**(원주 .xlsx zip + 괴산 64회 .xls — 자체검증 18/18·서술 조립 8/10 문자 일치, 불일치 2건은 골든 갈림: '총가구수'=일반가구수(2:1, 원주 골든이 오류)·세대당 자릿수. 웹 재배포판 재확보 경로 확립 — 천안만 스캔 PDF라 F-4 대기) · **원주 0500 채점 40.0% WRONG 0**(5.3 표 5/5 OK) · **표유출검사 `table_leak.py` ✅ 09-01**(증거인계 §4 두 갈래 구현·smoke_check ②-2 통합·되먹임 자동 스킵 — 기존 게이트 전부 통과였던 실유출 2건 적발: 천안 0100 조서 행정구역 셀 3개·0726 지정폐 표. W3 6파트 golden·재해 표본 수확 완료, 재해 장 슬러그 naming.md 확정) · **재해 distill 09-01 — rule 층 완성 — _category+파트 7장(1·2·3·4·5·6·7). **4·5장 계산 인풋 규약 명세 ✅**(유역×개발단계 값 묶음 8종·침사지 입경 매트릭스 — 표 구조 실측. 도구는 §7-3 대기, FARD2006 실측). 유역 n 관측 구간 2~3(천안 A·B — '1유역'은 마커 오판 정정) · **spec 3종 ✅**(1장 23·2장 9·7장 12토큰 — 전 문자열 골든 유일 검증. 핸들러 3종+되먹임 vars ✅ → Windows 베이스 배치 대기 지시서 ⑩) · **3종 대조 ✅**(재평=소재평 1:1 준용·검토서 5장 별도 rule, 기준=원주 태장동. 수확 함정 3종 — 서식 25곳 복사본·판 세대 혼재·재평 PDF 전용, `_variants.md` §4) · 원주 0500 재채점 36.7% WRONG 0(주거 뒤집기 예고 하락) · **커버리지 측정기 `coverage.py` ✅ 09-01**(사다리 점수 정의 — 유형 균등 23.6% · 실무 건수 가중 40.8%, 마일스톤마다 갱신) · **검토서 파트 rule 4장 ✅**(1장=개요 표+결정 조서로 소재평과 상이·2장 3분류·3장=재해이력/위험지구/방재시설 소싱 본체·4장=정성 분석, 3장 값 승계 원칙)**(`golden/small-disaster/_variants.md` — 골격 8장 2/2 일치·차이는 유역 n 가변 등 사업 고유·절번호 자동필드가 `楴䵴` 잔재로 남아 마커로 씀·서식 판 §0 판정) → [`20260831_윈도우_0500_결함수정.md`](docs/20260831_윈도우_0500_결함수정.md) | [`20260831_전체범위_작업전환계획.md`](docs/20260831_전체범위_작업전환계획.md) |
| ~~W~~ | ~~Windows — 지역개황 베이스 + §B 표~~ ✅ **완료 2026-08-24.** 베이스 토큰 **64종** · 표 37개 · 천안 **OK 44/83(53.0%) · 코드 오류 0** · 삽도 4/6 삽입 · 원주 흔적 **0건**. ⚠️ **서술 문장 유출 10건** — 표는 채웠는데 그 위 문장이 기준 사업 그대로였다. 채점 분모를 템플릿 토큰에서 뽑아 **안 뚫은 자리가 분모에서 사라진** 것이 원인 (`engine/score_regional.py` 가 정답에서 뽑도록 교체 · `engine/leak_check.py` 신설). ⚠️ **판이 바뀌면 표에 행이 생긴다** — 천안 매립 2행은 2024판 신규 등재였다 (`catalog/probe_landfill.py` 로 역추적). 정답지 누적량 `660,26.2` 는 자릿수가 깨진 값 · `[확인 필요]` 빨간 글자. 결과 [`20260824_결과보고_지역개황.md`](docs/20260824_결과보고_지역개황.md) · 검증 `cases/small-env/천안_화덕리/regional-overview/validation.md`. **남은 것은 판단이 필요한 5건** — ①표 삽입(소각시설) ②표 서식 변이(산업단지) ③절 도입 문장 ④삽도 채우기 ⑤좌표 출처. 논의 자료는 결과보고 §6, 실무자 질문은 [`실무자_확인요청.md`](docs/실무자_확인요청.md) **H절**(신규)·F-3·G-6 **09-01 지시서(0500+결함2건+수확) 전부 완료** — ①0726 성상별·기초시설 유출 0 ②원주 수질 vars 환각 정정 ③**0500 베이스 29/29 → 소환 11파트 전부 확보** (5.3 통계 소싱은 Mac `extract_0500`, 원주 재생성으로 반영) ⑤**수확 완료** — W3 6파트 txt `golden/원주_무장리/` 커밋(동식물상 205,209자 포함) · 재해 3종 표본 39건 603MB `raw_data/nas/disaster_samples/`(git 제외, txt 로컬 — 재해 폴더 구조는 Mac 결정 대기). 횡성 재평 **표지 실물로 유형 검증 통과**. 서식 판 ✅ Mac 판정(09-01): 둘 다 **고시 제2023-63호(2023.08)** 세대 — 확보분으로 진행, 신판 고시 여부만 실무자 확인 I-1. 🚨 `extract.py` — **짝 없는 서로게이트가 추출을 통째로 죽였다**(동식물상 73MB), `_sanitize()` 추가. 다음: Mac distill(W3·재해) · 남은 결함은 **0100 발전설비 외 없음**(이번에 해결) · 표 유출 검사 신설 제안 |
| 〃 |
| **0** | **★ 지역개황(`regional-overview`)** — ①선별 ②distill ③통계 소싱 ④카탈로그+추출기 ⑤7개 절 파서 ✅ 08-20 · **⑥통계 19종 중 15종 자동화 ✅ 08-24 (절 18/19)**. `stats_national.py`(전국 통계 9절·자체검증 82) · `stats_irregular.py`(선언으로 안 되는 모양 4종) · `stats_pdf.py`(**텍스트 PDF** 3종 — 합계 검산으로 표 복원) · `hwpx_table.py`(한글 표 읽기) · `xlsx_grid.py`(공용 격자) · `stats_registry.py`(자료 전수 목록) · `build_stats_values.py`(**값 저장소 + 판 지문**) · `trace_stats.py`(역추적기). **최신판을 발행처에서 직접 받는다** — 상수도 2024·하수도 2025·폐기물 2024·산업단지 2025-4Q. ⚠️ **출처 주석과 실제 사용 판이 다른 사례 4건** — 판 식별이 F-1보다 근본적이다. **다음은 vars 빌더 + 베이스 hwpx(Windows)** | [`20260824_지역개황_작업계획.md`](docs/20260824_지역개황_작업계획.md) · 미팅용 [`20260824_통계자동화_결과보고.md`](docs/20260824_통계자동화_결과보고.md) |
| ~~1~~ | ~~`[확인 필요]` 빨간색 표시~~ ✅ **이미 구현됨** — `generate.py` 저장 직전 `color_markers(hwp, [MISSING, MODELING])`. 산출물 charPr 에 `#FF0000` 확인 (09-01 검증) | 〃 §4 |
| 2 | ⏸ **절 삽입 기능 보류** → 미팅 요청 "단락 방향성"(수치 따라 단락 서술 방향 변경 or 방향 제안)과 통합 설계 검토 — 지역개황 진행 중 실전 소재로 | 〃 §1 · 청양 §5-1 |
| 3 | 대기질 AERMOD 가중치 처리 설계 — 모델링 출력이라 계산 불가. `[모델링 필요]` 로 둘지, 결과 파일을 인풋으로 받을지 | air-quality §2-5 |
| **4** | **카탈로그 v2 재빌드 + 통계 관리 체계** — NAS 업로드 완료(8월 말) 후: 기타자료 심층 포함 전수 크롤 → `build_catalog.py` 확장 → 전체 카테고리 지도. 미팅 요청 "중요 통계 체계 관리"를 여기 연계. **+ 베이스 수확 전 지역 확장**: v2 로 삽도 폴더 경로가 전수 확보되면 `harvest_sheets.py` 를 전 사업으로 — 단 **F-11 이 "도엽" 으로 확정된 뒤에만**(디지털이면 불필요), 그리고 실전 주력은 수확이 아니라 **도엽 캐시**(번호 정합 자동)임을 유지 | `nas_survey_2026-08-13.md` · 미팅결과 §2 |
| **5** | **환26-09 청주 미원면 대덕리 태양광 = 실전 시범 대상 — 🔒 봉인** (2026-08-19). 지역개황이 **이미 완성돼 있다**(108MB) — 시범 때 PP 인풋 확보 가능. **정답지를 미리 열지 않는다.** 짝인 환26-14 괴산 후평리는 유형이 달라(근린생활+재해) 골든셋 표본으로 돌렸다. 시범 실행은 **10월 말 전체 구현 + 실무자 미팅 이후** | `nas_survey` §3 · `20260819_…선별.md` §3-2 |
| 6 | **삽도 ✅ 주소 한 줄 → 완성 삽도** (08-20). `map_fetch.py` · `figure_overlay.py`(요소 15종) · `parcels.py` · `watercourse.py` · `ecology.py` · `admin.py` · `ecgy.py` · `psd_base.py` · `hydro.py`. **평창으로 정답과 실제 대조**(08-20) — **5종 전부 근접** · 다른 파트 1(식생보전등급도 — 동식물상 산출물 인용). 위성사진은 6종 밖(사업개요 파트)이나 **정온시설 배경**이라 함께 다룬다. **비교는 전부 정답과 같은 베이스**(NAS PSD 수확 도엽·캡처) 위에서 한다 — 지역개황도만 디지털(F-11 전시물). 키: VWorld(지오코딩·**연속지적도·하천망**) · NGII(지형도·**위성영상 WMTS**) · ECVAM(TMS) · EGIS(불필요). **08-20 해결 4건** — 위성사진을 카카오맵→NGII 로 대체(해상도가 오히려 높다, **약관 문제 소멸**) · **사업지 경계를 편입토지조서 지번 → 연속지적도로 자동 생성**(설계도서 불필요, 천안 8/8·청양 11/11) · 수계흐름모식도 입력 · **생태자연도**(EcoBank 키 → WFS 로 **등급 판정까지**, 골든셋 **8/8** — 다등급 `2, 3등급` 표기·저편입 임야 조각까지 재현 **본문 서술 근거**가 된다) · **행정구역명 라벨**(`admin.py`) · **수계도 흐름 화살표·하천명**(하천망 자료가 면형이라 **지도 그림의 하천 색을 읽어** 풀었다). ⚠️ 골든셋이 전부 같은 답이면 검증이 통과해도 약하다 — 생태자연도가 한때 8/8 로 보였는데 버그 둘이 서로를 가린 것이었다. **도엽 체계**(08-21·23): NAS 삽도 PSD 에서 깨끗한 베이스 수확(`catalog/harvest_sheets.py`) — 판별을 **불투명·명암·흑백마스크 세 줄**로 다시 그어 **49장**(골든셋 8건 전부). ⚠️ 재편 사업 경로는 한 단계 깊다(`환25-NN …/lsy {사업}/3. 삽도`) · **수확을 두 번 겹쳐 돌리면 PSD 가 깨진다**(락으로 막음). ⚠️ 채도로 지도를 가르면 **흑백 도엽 스캔이 죽는다**. `catalog/index_sheets.py` 가 이름을 종류로 통일(`지역개황도.png`) — ⚠️ 정렬로 정하면 `생태자연도 2` 가 `(최종)` 을 밀어낸다(`최종` 우선). **이미지는 커밋하지 않는다** — 다시 못 만드는 값은 `sheet_georef.json` 실측 좌표와 `catalog/review/sheets_harvest.md` 목록뿐(복원: `index_sheets.py --restore`). **좌표→도엽번호 계산기**(골든셋 8쌍 일치). 상수원보호구역 채색은 **VWorld `LT_C_UM710` 로 해결**. **생태·경관보전지역**(08-23): 공공데이터포털 `해양수산부_생태경관보전지역` WFS 로 해결 — `engine/ecgy.py`, 시군 판정 **8/8** · 평창 이격거리 **1.04km 정답 일치** · 채색 경계 겹침. ⚠️ 8건 중 7건이 "없음" 이라 *항상 0 을 내는 버그*도 7/8 을 통과한다 — 증명하는 것은 **평창 이격거리 한 줄**. ⚠️ WFS `maxFeatures` 없으면 **조용히 10건만** 온다(전국 33개소) · 짝인 시도 데이터셋은 부분집합이라 **안 쓴다**. 남은 것: **지정현황 표의 면적·특징·지정일자**(폴리곤 순면적 94.99㎢ ↔ 정답 표 79.259㎢ — 계산하면 안 된다) · ~~hwp 삽입 검증(Windows)~~ ✅ **완료 2026-08-25** — `engine/insert_figures.py` · 시험 그림 5장 PDF 확인, 비율 왜곡 0 | [`20260819_삽도_자동화.md`](docs/20260819_삽도_자동화.md) |
| **7** | **법령 개정 감시 — 아예 없다** ⚠️ 지역개황 법령·환경기준표 **13개**가 베이스(원주 2024)에서 그대로 복사되는데 개정 여부를 **확인한 적이 없다**. ⚠️ **골든셋 대조로는 못 푼다** — 숫자 있는 11표는 천안 정답과 값이 정확히 맞았지만(수질 21값·호소 30값·대기 14값 등) **완성 보고서도 같은 회사가 같은 베이스로 만든 것**이라 둘이 같이 낡았으면 통과한다(= *"골든셋이 전부 같은 답이면 검증이 통과해도 약하다"*). 표본을 늘려도 안 풀리고 **바깥 근거**가 있어야 한다. 사각지대 둘 더 — ①`score_tables` 는 **숫자만** 비교(조문 문구·지역명 목록 무시) ②`배출허용기준 지역지정`·`배출시설 설치제한지역` 은 **0값이라 비교 자체가 없다**(`not a and not b` → OK). 법제처 **국가법령정보 오픈API**(`law.go.kr`, 무료·이메일 신청)가 시행일자·개정이력·별표 본문을 준다 — 통계의 **판 고정 + 신판 감시** 구조를 그대로 옮기면 된다. **① 인용 법령 목록 뽑기(반나절) → ② 시행일자 대조 → ③ 별표 본문 대조**. ①만 해도 위험 규모를 안다 | 결과보고 §4-2 ⑧ |
| **8** | **생태·경관보전지역 지정현황 표 — 자료는 있는데 넣을 자리가 없다** ⚠️ 천안으로는 안 드러난다. 자료·파서·vars·종합표 ○× ·서술문장까지 다 됐는데 **표를 채우는 코드가 없다** — `generate.py` 의 `LIST_TABLES` 에 없다. 더 근본적으로 **베이스(원주)에 그 표 자체가 없다**(원주는 지정 없음 → "없는 것으로 조사되었다" 문장만). 평창 정답에는 `지역명·위치·면적(㎢)·특징·지정일자(변경일자)` 5열 표가 통째로 들어간다. **할 일 2(절·표 삽입)와 같은 부류**이고, 골든셋 8건 중 평창 1건에만 있어 **천안 채점으로는 영원히 안 잡힌다**. 값은 준비돼 있다 — `동강유역 · 79.259 · 지형경관 우수 · 2002.8.9('19.12.23확대)`. ⚠️ 기준월에 따라 면적이 갈린다(`'24.5월` 80.426) → **G-1**. ⚠️ 베이스 문장은 `'24.5월 기준` 을 인용하는데 평창 정답은 `'23.12월 기준` 이다 — **출처 주석도 같이 바뀐다** | 확인요청 G-1 · 결과보고 §4-2 ③ |
| **10** | **`수질오염총량 단위유역` 절 — 천안 정답에 있는데 통째로 빠진다** ⚠️ 베이스(원주)에 그 절이 없다. **문장 1 · 삽도 1(`수질오염총량 단위유역도`) · 종합표 행 1** 이 한 세트다. 골든셋 8건 중 **2건**(천안·충주)에만 있고, 충주는 앞에 `비점오염원관리지역` 이 하나 더 있어 **절 문자가 `사`↔`아` 로 밀린다**. 할 일 2·8 과 같은 부류(절·표 삽입). ⚠️ **채점이 삽도를 6종 고정으로 세어 분모에서 조용히 빠져 있었다** — 정답에서 뽑도록 고쳤고 83 → **84항목**이 됐다 (`OPTIONAL_FIGURES`) | 결과보고 §4-2 ⑦ |
| 11 | 표준 인풋 리스트 — 전 파트 확대 후 rule 변수표에서 일괄 집계 | 〃 §3 |

<details><summary>2026-08-03~04 완료분 (윈도우 세션)</summary>

표 29 가설방음판넬 열 ✅ · 표 21·24 구분 거리 vars ✅ · 법령표 9·10·11 서식 ✅ ·
출처 주석 `clone_para()` ✅ · 청양 vars 정정 ✅ · 레이아웃 육안 확인 ✅ ·
캡션 `KeepWithNext` ✅ · PP 라벨 vars ✅
</details>

> 육안 확인은 **`engine/to_pdf.py` → PDF → 페이지 이미지** 로 자동화했다 (`/validate-report` 6단계).
> 가장 걱정했던 표 행 확장(7·8·10행)과 절 삭제(`delete_range`)는 **문제가 없었다.**
> 실제 결함은 예상 못 한 자리에서 나왔다.

### 자료 소재 — 2026-08-03 NAS 재조사

| 사업 | 상태 |
|---|---|
| **괴산 삽도** | ✅ ~~NAS 에 없음~~ → **`golden/…/괴산_금신리/원본.hwpx` 의 BinData 안에 2장 다 있다.** 보고서 HWPX 는 ZIP 이라 삽도가 동봉된다 |
| **청양** | ✅ **막힘 해소.** 삽도는 NAS `lsy/…/8. 청양 매곡리/3. 삽도/`, **PP 10개는 `(본안) 0200 지역개황` 에 텍스트 표로** 있다 (삽도 판독 불필요) |
| **여주** | ❌ 측정보고서가 NAS·아카이브 어디에도 없다. rule §4-3 상 **인용 사업이라 자체 측정을 안 했을 가능성** — 필요한 것은 인용원 사업 자료다 |
| **신규 3건** (2026-08-04) | ✅ 옥천 사양리(2024 야영장) · 청주 호명리(2024 태양광) · 평창 수청리(2024 태양광). NAS `2024/*/2. 보고서/(본안) 0727 소음진동*.hwp` — **`0727` 로 검색하면 파트별 보고서가 바로 나온다** |
| **대기질 4건** (2026-08-05) | ✅ 천안·옥천·청주·평창 `(본안) 0722 대기질*.hwp` 확보 → `golden/*/air-quality.txt` |
| **NAS 전수 조사** (2026-08-13) | ✅ 실규모 폴더 7.4만+(기타자료 심층 6.7만+ 별도)·파일 73만+·`(본안)` 8,047건 — **7장 파트별 각 165~234건 (수질 0723 183 포함, 어느 파트든 자료 충분)**. 번호는 장·절 코드, 수리수문 낀 사업 27건은 밀림. 신규 유입은 `0. 평가서` 환25·26 사업 폴더 (**환26 = 실전 시범 가능 후보**). 병렬 크롤러·`nas_diff.py` 구축 → `catalog/review/nas_survey_2026-08-13.md` |
| ⚠️ 파트 번호 | 소음진동 `0727` 10건 : `0728` 3건 — **수리수문(0724)이 끼면 밀린다.** 번호는 사업 목차로 확인 (`docs/naming.md`) |
| **지역개황 골든셋** (2026-08-19) | ✅ 8건 확보 (`golden/*/regional-overview.txt`). ⚠️ **NAS 경로가 이미 바뀌었다** — `lsy/3. 소규모환경영향평가/2024·2025` 가 `0. 평가서/환경/환25-NN …/lsy {사업}/` 로 이동. 08-13 스냅샷이 부분 무효라 카탈로그 v2 전에 `/nas-survey` 재실행 필요. ⚠️ 원본이 47MB~**1.4GB** (삽도) — `golden/` 에 원본 동봉 금지, txt 만. ⚠️ **파일명 `(완)` 을 믿지 말 것** — 청양 0200 은 내용이 강릉시였다 (본문 지자체명 검사 필수) |

### 판단 대기 — **실무자 확인이 필요하다**

> 📋 **질문지: [`docs/실무자_확인요청.md`](docs/실무자_확인요청.md)** — 이대로 물어보면 된다.
> 코드·지식으로 갈 수 있는 곳은 다 갔다. 남은 것은 **회사 표준이 없어서** 갈리는 항목들이다.

| 구분 | 항목 |
|---|---|
| **통계 판·열** | **F-4 통계연보 입수 경로**(지역개황 최대 병목 · 6절 · 지적통계 우회로 포함) · **G-1 판 고정 시점**(가장 중요) · **G-2 일평균↔일최대**(골든셋 **2:1** — 원주·괴산 일평균 ↔ 천안 일최대, `18,310+3,910=22,220`=2021판 일최대 합계) · G-3 취수장 상세주소 · G-4 산업단지 면적 단위 · G-5 표준 통계 창고 · **G-6 극점 좌표** — ✅ **VWorld 경계로 계산 가능** 실증(`catalog/probe_extremes.py`, 지명 3/4). 남은 것은 ①**측지계**(정답이 폐지된 **동경측지계** — 변환하면 오차 −9″→+0.7~2.3″) ②**연장거리 정의**(남북 39.2↔38 ✅ · 동서 36.7↔**29** ❌, 정답 자기 좌표로 계산해도 36.7) |
| **표기** | 측정일시 월 반복 4:3 · 측정지점명 4종 · 이격거리 단위 3:1 · 환경보전목표 행 이름 4:3 · 영향 서술 4:3 |
| **계산** | **반올림 경로**(괴산↔청양 상반, 판정을 뒤집는다) · 분산투입 감산량(1.7/3.3/4.9) · **상회 0건일 때 최종표 2:2** · 방음판넬 규격 |
| **자료 부재** | 조사시기 · 일 작업량 · XTM/YTM · 시설명 · **PP 이격거리**(삽도 판독 평균오차 24m — 지역개황편 표가 있으면 9/10 정확) |

---

## 코드 작성 시

- Python 3.10+, 가상환경 `.venv/`
- 한글 API 사용 시 반드시 `.claude/rules/hwpx.md` 참조
- **엔진은 파트를 몰라야 한다** — 파트 고유 정보는 코드가 아니라 `cases/*/vars/` 와 rule에 둔다
- 원본 HWP/PDF는 `raw_data/` (git 제외)
- **`docs/` 신규 문서 파일명은 `YYYYMMDD_제목.md`** (예: `20260813_미팅체크.md`)
- **구조가 바뀌면 README 를 같이 고친다** — 폴더·스킬·엔진 파일 추가/삭제, 측정 수치 갱신,
  로드맵 단계 변동 시 `README.md` 의 해당 절(트리·현재 상태·로드맵)을 같은 커밋에서 동기화
- Python XML 직접 조작 방식은 **파일 무결성 문제로 중단** (한글 API만 사용)
